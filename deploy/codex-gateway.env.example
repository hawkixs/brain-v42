# Install as ~/.config/brain-v42/codex-gateway.env with mode 0600.
# Generate a fresh secret; the gateway rejects placeholders and values under 32 bytes.
BRAIN_CODEX_GATEWAY_TOKEN=REPLACE_WITH_RANDOM_TOKEN
