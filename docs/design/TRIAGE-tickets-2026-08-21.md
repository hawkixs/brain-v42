# TABLE DE DISPOSITIONS FINALE — triage tickets brain-v42, 2026-08-21

**Périmètre mesuré ce jour** (`psql`, arbre principal à `14c0385`, prod à `045`) : **41 tickets vivants** dans l'orbite brain-v42 = 36 adressés à `brain-v42` + 5 sortants depuis `brain-v42` (`9ef5c69d`→auto-discord, `fbacb250`/`e45976c0`/`a1090d46`→red-codex, `89140780`→red-shrik). **+1 objet** en `resolved` en attente de confirmation (`40dbfeb1`). **39 tickets triés**, 3 non triés (voir angles morts).

Règle de comptage : une seule disposition primaire par ticket ; les actions secondaires sont annotées sur la ligne. `verdict_juge` conservé pour audit.

---

## 1. À FERMER — 4 tickets (uniquement les verdicts CONFIRMED par un sceptique)

| # | ticket | geste | preuve en une ligne |
|---|--------|-------|---------------------|
| F1 | `fbacb250` Codex Desktop provisioning (→red-codex) | **CANCEL** (jamais `resolve`) — opérateur, S | Consommateur disparu : 0 session Brain `client_key` codex depuis le 2026-08-11 (228 au total), 0 fichier `~/.codex/sessions` depuis 2026-08-11 09:58, seul rail vivant = `codex exec` (`scripts/dream/codex_runner.py:1,159`), 0 message red-codex en 20 j. |
| F2 | `2bd14b24` Idée — cycle de vie sessions vs subagents | **RESOLVE** — orchestrateur ops, S | Ticket de CADRAGE, cadrage fait et signé 4× en base : `1bc19052`, `ee271fbf` (2026-08-19), `c5160259` (2026-08-20), `567f6298` (2026-08-21) ; `DOSSIER.md:486` et `:503` le nomment. |
| F3 | `d30cf6e5` Ancrage refonte PROJETS+SESSIONS | **RESOLVE** (+ fusion vers `94738f25`) — orchestrateur ops, S | Mandat consommé : `docs/PROJECTS_SYSTEM.md` (234 l., `e04df96`) reprend littéralement ses 4 briques, et le gate « NE PAS DÉMARRER sans cadrage » est levé par `c5160259`/`9d22bc6a`/`567f6298` (toutes `active`). |
| F4 | `40dbfeb1` Onglet Brain red-monitor (→red-monitor) | **CONFIRM** `resolved`→`closed` — orchestrateur ops, S | Déjà `resolved` depuis 2026-08-11 ; les 4 demandes vérifiées dans le **bundle servi** (`assets/Brain-CYLdNcGV.js`, 11 chaînes correctives) et non seulement en source ; `red-server` MainPID=996570, `NRestarts=0` depuis le déploiement. |

### F1 — message de fermeture (3 corrections obligatoires appliquées)

> Fermé le 2026-08-21 pour **DISPARITION DU CONSOMMATEUR, pas pour correction** — le défaut n'a jamais été rejoué. `80e92492`, le seul travail que cet incident bloquait, est closed depuis le **2026-08-02 00:05** (le 2026-08-13 n'est que son `updated_at`) — et il a été livré **PAR CETTE MÊME SURFACE**, après récupération transitoire : threads `019fbfa3` (implémentation), `019fbfad` puis `019fbfb8` (revues), `019fbfbd` (intégration), SHA `8c436aa7`. Le défaut est donc **intermittent, non corrigé**. Aucune session Brain à `client_key` codex depuis le 2026-08-11 (228 au total) ; corroboré hors Brain : aucun fichier écrit sous `~/.codex/sessions` depuis le 2026-08-11 09:58. Le seul rail codex encore vivant est `codex exec` (`scripts/dream/codex_runner.py`), qui n'emprunte ni `create_thread`, ni `list_projects`, ni `set_thread_archived` — aucun appelant de ces primitives dans l'arbre. Aucun message red-codex sur ce ticket en 20 jours ; le dernier message red-codex tous tickets confondus date du 2026-08-01 16:46, soit 4 h 37 **avant** la création de ce ticket : l'amont ne l'a jamais lu. **On ferme pour absence de consommateur, pas pour retrait de la surface** : l'app-server codex tourne toujours (PID 2999, unité `~/.config/systemd/user/codex-app-server.service`, codex-cli 0.147.0) — reprendre cette surface demain ne demanderait aucun re-provisioning. À garder si le sujet revient : timeout borné et résultat terminal sur toute opération de tâche, statut observable par `clientThreadId`, cancel/reconcile idempotent, et la perte de terminal au second tour (`CreateProcess ENOENT`, reproduite sur `019fc035…` et `019fc042…`) avec son workaround mesuré. Le volet hygiène d'archivage/worktree reste porté par `a1090d46`. Rouvrir plutôt que recréer.

### F2 — message de fermeture

> **RÉSOLU comme ticket de CADRAGE, le 2026-08-21.** Les trois questions qu'il posait sont tranchées et signées : (1) rituel de fin → piste (a), **deux natures de session**, `agent` traçante auto-fermée sans rituel et `operator` avec rituel (tranchée le 2026-08-19) ; (2) subagent → **HÉRITAGE** de la session du porteur, réglé **par la mesure** et non par arbitrage — aucun des trois en-têtes ne distingue un subagent de son porteur ; (3) cycle automatique → auto-ouverture sur la clé (projet, CONNEXION) **en HTTP uniquement — pas de session automatique en stdio** (`c5160259` b) et fermeture d'inactivité `closed_inactive` par le sweep nocturne. Signatures durables en base, indépendantes de tout fichier : `1bc19052` et `ee271fbf` (2026-08-19), `c5160259` (2026-08-20), `567f6298` (2026-08-21 : `nature VARCHAR(16) CHECK ('agent','operator')`, `closed_inactive ⇒ nature='agent'` dans le CHECK terminal). Le mode d'échec faux-vivant qui a ouvert ce ticket est fermé empiriquement : sweep WET depuis le 2026-08-19 (`killswitches.conf:62,65`), 21 abandons `auto_stale_7d` le 2026-08-20, **zéro** session ouverte de plus de 7 jours aujourd'hui contre 39 ramassées à la main le 2026-08-06. L'IMPLÉMENTATION n'est pas faite et n'est pas portée par ce ticket : elle vit dans la migration 046 (M-A+M-G), porteur `94738f25`.

*(Levée de condition : le sceptique a établi que `1bc19052`/`ee271fbf` portent l'arbitrage **en base** — publier `docs/design/` n'est PAS un préalable à cette fermeture.)*

### F3 — message de fermeture (2 corrections rédactionnelles appliquées)

> **Ancrage rempli, puis remplacé.** Le point de départ que ce ticket devait fixer est documenté — `docs/PROJECTS_SYSTEM.md` (234 l., commit `e04df96`, branche `test/phase0-session-anchors`) et le corpus `docs/design/refonte-projets-sessions/` (ADR, PLAN, DOSSIER 645 l., SEQUENCEMENT, SPEC-checkpoint, SPEC-M-G, SPEC-pool-brouillons, baseline). Le cadrage que ce ticket exigeait AVANT tout démarrage est signé : `c5160259`, `9d22bc6a`, `567f6298`. **Le chantier vivant est suivi par `94738f25`**, qui porte l'état courant. Les douleurs sessions raccrochées (`2bd14b24`, `d04dc588`, `7ffe0e8a`) le sont **dans l'ADR (§ grille B, l.576-585)**, pas dans le fil de `94738f25`. Les quatre résidus du fil n°2 sont pliés : `expected_session_indexes` au SEQUENCEMENT l.63/231, parité `v4.sql`/`v4-pgrestore.sql` et `SESSION_INDEX_DEFINITION_MD5` au SEQUENCEMENT l.232/235, décision d'index MESURÉE ET CONCLUE au PLAN l.1432, spike B8 rejoué sur Claude Code 2.1.234. Le stock de fantômes est retombé à 9 ouvertes / 0 balayable. Aucun état des lieux à refaire : **les DOCUMENTS font foi, pas ce fil.**

*(Réserve à remonter, non bloquante : `PLAN:604-616` porte encore « trois colonnes nullable … `started_by_actor` créé sans index » sans encadré d'amendement, en contradiction avec `PLAN:1432`.)*

### F4 — message de confirmation

> **CONFIRMÉ le 2026-08-21, contre-vérifié aux deux bouts et non sur parole.** Code : `BrainActivity.jsx` porte les deux groupes à colonnes différentes (:101, :135), le groupe vide qui garde son en-tête (:108-110), « tentative » et jamais « appel » (:32-34, :72), `_process` masquée mais comptée (:87, :144-146), l'en-tête réécrit — jointure « prouvée » et « pas branchée », zéro occurrence d'« impossible » (:24-28) —, la mise en garde acteur/session non prouvés (:174-177) ; le **tri global avant filtrage** est à `brainPresentation.js:145-157` (`visible.sort(byRecency)`, `POSITIVE_INFINITY` pour la récence non mesurée, `localeCompare` en départage). Le remplacement demandé au message 1 est adopté : `client?.kind === 'session'` (:84), épinglé par `brainPresentation.test.js:35` et `Brain.test.jsx:365-371`. **Runtime, dix jours après votre déploiement** : `red-server.service` active, MainPID 996570 et `ActiveEnterTimestamp` 2026-08-11 21:23:24 CEST — le PID et l'heure annoncés au message 4 —, `NRestarts=0`, :8081 → 200 servant `assets/index--Tui5Tgp.js`, chaînes correctives vérifiées dans le chunk `assets/Brain-CYLdNcGV.js`. Le point 1(c) laissé de côté : accepté, votre raison est la bonne. **VOTRE MESSAGE 5, PRIS ET RENDU** : vous avez raison, un transport n'est pas un appelant et aucune présentation ne le rattrape. Nous prenons l'option (a) — retirer le transport du registre vif sur `DELETE /mcp` — parce qu'elle n'exige aucun changement de contrat chez vous. **Elle n'est PAS faite** : `client_activity.py` n'a aujourd'hui aucun chemin qui retire un transport avant son TTL de 600 s (`:43`), et aucun récepteur de fin de transport n'existe. Nous l'ouvrons en ticket brain-v42 distinct plutôt que de rouvrir celui-ci, qui n'est pas de la présentation. Votre point 6 sur red-lab est déjà notre ticket `a3fa6696` (cause intacte : `brain_client.py:30`, `Authorization` seul ; service simplement arrêté, d'où votre seau `unknown` vide) : nous le portons, ne l'ouvrez pas de votre côté.

---

## 2. À FUSIONNER — 1 fusion confirmée (+1 fusion REFUSÉE à acter)

| direction | statut | geste |
|-----------|--------|-------|
| `d30cf6e5` → **`94738f25`** | **CONFIRMÉE** par le sceptique (couverture intégrale vérifiée : les 3 pointeurs et le « vécu du jour » sont absorbés par `DOSSIER.md:79,82,220,237-238,276-285` et `ADR:576-585,1067,1809`) | La fusion s'exécute PAR le `resolve` de F3 + note miroir sur `94738f25` (**aucun des deux ne référence l'autre en base aujourd'hui** — le message de résolution crée le seul lien) |
| `b68b9692` → `259cfbe5` | **REFUSÉE** (verdict sceptique REFUTED) | **Aucune fusion.** Raison : la cible ne couvre que « deadline + cap du briefing » ; 4 des 6 demandes du doublonné (readiness, dépendances, raison/preuves, gate direct non autoritatif) en sont absentes, et `259cfbe5` **argumente CONTRE** un rang posé à la main (« la solution à NE PAS retenir »). Le juge du lot 4 proposait la fusion **inverse** : les deux directions s'annulent. |

**Disposition de remplacement pour le couple `b68b9692`/`259cfbe5`** (2 messages de liaison, orchestrateur ops, S) : `b68b9692` **cède** son volet « priorité » à la piste échéance de `259cfbe5` et **conserve** readiness + dépendances + raison/preuves + gate direct ; `259cfbe5` porte l'**unique** `ADD COLUMN` sur `tickets` (lot C12) et doit être relu avec le périmètre de `b68b9692` sous les yeux. Ratification chiffrée du critère d'acceptation 3 : `features.pinned` = 45/193 (brain-v42), 88/896 (global), 2026-08-21.

---

## 3. ACTIONNABLES — 16 tickets, priorisés (mission courante d'abord : prod 046 → git clean/e2e → durcissement → dette)

| rang | ticket | action de tête | effort | exécutant | gate |
|------|--------|----------------|--------|-----------|------|
| **A1** | `eb067b57` Recovery v5 | Minter `ops/recovery/brain-v42-v5.{json,sql,-pgrestore.sql}` — **25 checks, `alembic_head` DÉRIVÉ** (précédent `test_alembic_env.py:254-259`), re-mintés APRÈS la 046 ; `test_recovery_contract_v5.py` (gel byte-exact + SHA-256 des actifs v4) ; corriger les portes d'attestation contradictoires de `PLAN_INDEX_REPAIR_RUNBOOK.md` (l.63/66/583 disent 25/25, l.154-155 disent 24/25) | **L** | worker code (worktree `feat/session-nature-046`) | **chemin critique 046** ; NE PAS appliquer la recette « lignes 1789/1791 » du fil du 08-06, caduque depuis S1 ; **rejouer** l'attestation en `BEGIN READ ONLY` — le 22/25 du 08-20 est daté |
| **A2** | `74ab1931` DR v3 — **lot L1 seul** | **Déplacer `~/.config/brain-v42/dr/PRIVATE-KEY-A-DEPLACER-HORS-DE-CET-HOTE.asc` hors de cet hôte et supprimer l'original** (3478 o, mtime 08-16 inchangée) | **S** | **opérateur** | aucun — à faire tout de suite ; L2/L3/L4 ci-dessous |
| **A3** | `4a6fe67e` Dedup features | `systemctl --user restart brain-metrics` + vérifier que le nouveau PID est postérieur à `64caf89` — **c'est ce geste, et lui seul, qui arme la garde `pinned` en prod** (PID vivant 1282863, démarré le 2026-08-14, deux jours AVANT la garde) | **S** | orchestrateur ops | réarme le compte à rebours de 6 h, cette fois sur du code gardé ; poster le recadrage « défaut 1 corrigé, non armé ; défauts 2 et 3 ouverts » |
| **A4** | `a1090d46` **spin-off git-clean** | Mesurer les **448 refs `refs/codex/snapshots/*`** en entier (`for r in $(git for-each-ref --format='%(refname)' refs/codex); do git merge-base $r main >/dev/null 2>&1 \|\| echo $r; done \| wc -l`) et poser leur sort **dans l'étape CLEAN GIT signée** — 40/40 échantillonnées sans merge-base, alors que les 4 branches en ont toutes une : **le geste tel qu'écrit (« branch -D des 10 branches sans merge-base ») ne toucherait pas le vrai porteur de la lignée pré-réécriture** | **S** | orchestrateur ops | **AVANT** l'étape CLEAN GIT du focus rev 214 |
| **A5** | `9e1a2b39` CAPABILITY_ENFORCEMENT | (1) Trancher la primauté `Environment=` vs `EnvironmentFile=` **empiriquement** avec la sonde déjà présente à `tests/integration/test_dream_systemd_install.sh:385-420` ; (2) poser `Environment=BRAIN_DREAM_CAPABILITY_ENFORCEMENT=true` en fin de `killswitches.conf` avec commentaire disant que **la source de vérité reste `mcp-token.env` (`token.conf:5`, trie APRÈS, prime)** et que cette ligne est un **miroir de revue**, pas un armement ; (3) `daemon-reload` | **S** (~15 min) | orchestrateur ops | aucun redémarrage ; effet à la nuit suivante ; **ne jamais composer avec `echo`, ne pas toucher `mcp-token.env`** |
| **A6** | `0850cc37` Postgres CI | **Contre-vérification sceptique obligatoire** (cf. angles morts) puis `resolve` + ouvrir le successeur étroit « `test-coverage` mesure 85,29 % sans 55 tests adossés à une base » : recopier `services.postgres` + `BRAIN_V42_TEST_DB_URL` + `alembic upgrade head` dans `test-coverage`, et **INVERSER** le témoin `test_github_workflows.py:225-239` au lieu de le supprimer | **S** puis **S** | orchestrateur ops → worker code | le corps ne peut pas être re-titré (`cabb7503`) : « actionnable et faux » est la pire combinaison |
| **A7** | `191b2dba` Revue 2026-09-03 | Poster le **garde-fou** : l'étape 3 (« si zéro ligne : fermer ») est piégeuse — la fenêtre `run_date > '2026-08-20'` rend zéro ligne **quand aucune nuit n'a tourné dedans**, ce qui est le cas aujourd'hui. Exiger un dénominateur ≥ 7 nuits d'extract. Consigner ici le résultat du matin du 21/08 comme ligne de base | **S** | orchestrateur ops | **ne rien fermer avant le 2026-09-03** |
| **A8** | `d45689a3` Rollout shim embedding | (1) `docker tag brain_v42-embedding-shim:latest …:pre-sec2-20260706` **avant** tout rebuild (le rebuild écrase la seule copie de juillet) ; (2) fenêtre hors nuit dream et hors 046 ; (3) override `ports:["0.0.0.0:8003:8003"]` pour ne livrer que les **limites**, puis retrait de l'override pour le **bind** ; (4) canari `/embed` + `/rerank` + un `brain_search` réel ; (5) 24 h de surveillance | **M** | orchestrateur ops | **GO opérateur explicite** (mutation prod hors bande : si le shim casse, `brain_search` et le rerank tombent — skill `debug-brain-v42-embedding-500`) |
| **A9** | `b93e32be` access_log | (a) **Chiffrer enfin le flux** sur 24 h (deux relevés de `sum(access_count)` sur les six tables), pour produire le coût de stockage que le ticket réclame depuis l'ouverture. Fait neuf à porter au fil : `pg_access_log.purge_old(days=30)` (:175-183) **existe déjà** et n'a que son test unitaire pour appelant | **S** | orchestrateur ops | dette **strictement croissante** : 754/718 → **837/807** en 5 jours ; arbitrage (b) = opérateur, **aucune écriture avant le rendez-vous 046** |
| **A10** | `1c40c36a` ActivityReporter | Un commit : émetteur **coalescent** (dict agrégé sur (actor, session, transport), tâche pompe, `MAX_OBSERVATIONS=64`, `calls: N`) — referme A et B ensemble sans toucher au sidecar | **M** | worker code | contrat feu-et-oubli inchangé ; la sonde anti-tautologie doit rester verte ; **NE PAS livrer à deux heures d'une nuit à dix projets**. Repli **S** : ne batcher que B |
| **A11** | `a3fa6696` red-lab X-Brain-Agent | (1) **hors dépôt, service arrêté** : ajouter `"X-Brain-Agent": self.author_project` à `brain_client.py:30` puis redéployer `/opt/red-lab/current` ; (2) **dans ce dépôt** : corriger `docs/superpowers/specs/2026-08-06-live-client-activity-design.md:245` (« basename du répertoire de **LANCEMENT**, pas un nom de projet ») | **S** + **S** | opérateur (1) / worker code (2) | fait neuf au fil : l'ADR crée `started_by_actor VARCHAR(64)` (l.692) — dès la Phase 1 un client sans en-tête écrit `unknown` dans une **colonne durable** |
| **A12** | `2547b4a2` apply/reject curation MCP | Recadrage (le stock A BOUGÉ : 582→88 proposed, **+543 rejets, 0 apply**, écrits **en SQL brut hors surface**) puis **lot A** : tools MCP `apply`/`reject` scopés par jointure `features.project_key`, `project_key` requis, mutation sur `proposed` seul, pas d'« appliquer tout » | **S** + **M** | orchestrateur ops → worker code | **lot A sans migration → jouable pendant la 046** ; **lot B** (colonnes d'audit du rejet) exige une tête → **interdit** tant que la 046 est en vol. Confirmation opérateur requise : un reject non horodaté est-il acceptable en attendant ? |
| **A13** | `cabb7503` Édition de ticket | `brain_ticket_edit(ticket_id, author_project, title, body, reason)`, réservé aux participants, qui **(a) n'écrit PAS `updated_at`** (aucun trigger ne l'y force — mesuré) et **(b) journalise systématiquement l'ancien texte en fil** | **M** | worker code | **aucune migration** ; soumettre (a) et (b) au fil comme réponses aux 2 questions du corps avant d'exécuter. Variante moins-disante : titre + bandeau « PÉRIMÉ — voir fil », corps immuable |
| **A14** | `af3b58dd` Hygiène MCP | (a) `brain_set_project_context` → modèle Pydantic unique (16 params à plat) + test de compat à 16 kwargs ; (b) `brain_propose_adr` → sortir le chemin dream-promotion pour que le XOR (l.440-443) devienne une propriété de schéma ; (c) `brain_list_adrs` | **M**+**M**+**S** | worker code | (b) exige une décision de dépréciation (les prompts dream l'appellent) ; (c) exige une décision alias vs retrait — noter que sous profil `compact` la duplication n'est visible que de `brain_find_tool`, et corriger la phrase du fil qui nomme `list_adrs` comme tool |
| **A15** | `89140780` run_qa (→red-shrik) | Rendre URL et modèle explicites et configurables, corriger le docstring l.4 (topologie `red-data/nomic` disparue), test anti-IP-en-dur. **Retirer du périmètre** le volet authentification : l'endpoint `192.168.1.11:8003` est **injoignable** (curl timeout rc 124) et le dev-pc tourne Windows 11 | **S** | worker code red-shrik | aucune décision préalable ; la question « retirer / dormant / repointer avec baseline de parité » se pose **sans** l'attendre, défaut recommandé = dormant |
| **A16** | `b68b9692` Readiness (ex-doublon **REFUTED**) | Rescoper : readiness + dépendances + raison/preuves + gate direct avant lancement (inventaire ou sous-agent **non autoritatif**) ; céder « priorité » à `259cfbe5` ; poster les 2 messages de liaison | **S** (liaison) puis **M** | orchestrateur ops → worker code | **retour en actionnable parce que le doublon a été REFUTÉ** ; migration derrière la 046 (« jamais deux têtes en vol ») |
| **A17** | `b3331691` Préflight gateway | Étendre `_GATEWAY_CONTRACT_PROOF` (`rotate_codex_gateway_credentials.py:178`) aux **3 clauses manquantes** (`security_barrier`, 2 triggers) en lisant les mêmes prédicats que `composition.py:61-79` ; **écrire l'arbitrage de layering** (duplication + test d'agrément, ou SQL en fichier de données partagé) ; lier `_CODEX_GATEWAY_CONTRACT` à `CONTRACT_COLUMNS` ; retirer le commentaire faux `test_rotate_…:1241` | **M** | worker code | **avant tout rollout de la gateway**, pour que la première rotation soit gardée ; urgence nulle aujourd'hui (aucun conteneur, `curl :9211/ready` rc 7) |

*(A17 porte le rang 17 : 16 tickets + le spin-off A4 qui n'est pas un ticket. Total tickets actionnables = **16**.)*

**Lots différés rattachés** (à ne pas perdre) : `74ab1931` L2 off-host automatisé avec rétention distante — **worker code dans red-backup, L**, prérequis dur **S** = mesurer le taux d'allumage du dev-pc sur 7 jours ; L3 drill de rebuild Neo4j — worker code, **M** ; L4 propager v5 dans `red-backup/config/recovery/` et délier les `Literal` v1/v2 de `src/backup/recovery_contract.py:213` — orchestrateur ops, **M**, **après** A1.

---

## 4. BLOQUÉS OPÉRATEUR — 7 tickets, 6 questions (une phrase chacune)

| # | ticket(s) | question exacte | après signature |
|---|-----------|-----------------|-----------------|
| **Q1** | `52d6b319` | « M'autorises-tu à installer le helper privilégié (`sudo ./deploy/install-brain-shrik-env-control.sh`, présent mais **jamais installé** depuis 20 jours) ou à poser la règle sudoers bornée, sans quoi le cutover gateway `:9211` reste mort ? » | ops **M** : rotation coordonnée + `docker compose --profile codex-gateway up -d` + preuves readiness/9 vues/mutations/409/scoping/refus des anciens credentials. En parallèle, sans attendre : worker code **S** — corriger le titre (`:9210`→`:9211`) et purger les affirmations head-037 ; **extraire le rattrapage UI red-codex en ticket propre** (jamais fait, msg 12 et 32) |
| **Q2** | `e45976c0` **+** `a1090d46` (question unique) | « red-codex est mesuré inerte depuis le 2026-08-05 (`project_contexts.updated_at`=2026-08-01, `local_path` vide, 0 message de fil sur 4 tickets ouverts) : (a) staffer, (b) acter « UNAVAILABLE est permanent » et annuler les deux sortants restants, ou (c) **mise en veille datée** avec date de revue — laquelle ? » | Recommandation **(c)** sur le gabarit signé du poison pill (`74bf3e6f`) : revue datée + requête de surveillance. **Cohérence de famille à valider explicitement** : `fbacb250` tombe seul parce que son consommateur a disparu, alors que ces deux-ci portent des contrats **encore valides et fail-closed** (routage/coût interdits hors VERIFIED ; critère (2) de `a1090d46` de facto tenu par 448 refs) — rien ne casse tant qu'ils dorment |
| **Q3** | `ac3166d9` | « Ce ticket passe-t-il en `resolve_pending` (partie locale livrée, le reliquat est host-owned et appartient à `e45976c0`) ou en `cancel` (le consommateur a disparu) ? » | **S**, aucune ligne de code côté brain-v42. Remesuré à codex-cli 0.147.0 : `AttestationGenerateParams` = `{}`, `AttestationGenerateResponse` = token opaque, `Thread` sans `model` ni `effort`. **Dans les deux cas, retirer `in_progress`** — il décrit l'attente d'un tiers |
| **Q4** | `d9a968f7` | « Unifier `FeatureLinker` sur `ClusterGuard` (A — ferme l'incohérence, mais touche un chemin **quiescent depuis 11 jours** pesant 6 % des liens, sans aucun symptôme qui le réclame) ou assumer le repli documenté (B — test qui nomme la divergence, WARN au lieu de debug, et le plafond `max_links` que `_do_link` **n'a pas du tout**) ? » | **(B) recommandé** : worker code **S**, TDD, aucune migration, jouable pendant la 046. (A) = **M**, à ne pas lancer dans le couloir 046. Chiffres d'arbitrage prêts : 56 rattachements multiples / 3784 (1,5 %), aucun depuis 30 jours ; 3623 liens guard vs 232 bruts |
| **Q5** | `51455b48` | « Statu quo sur `DEFAULT_MODEL` mort en 410 (coût = un aller-retour 410 par run + une ligne d'avertissement, repli mesuré) ou promotion de `meta/llama-3.3-70b-instruct` en primaire, en assumant qu'aucun canary sur le vrai prompt d'extraction n'aura été joué ? » | (b) = worker code **S** (une constante + un test). En attendant, fil : instrumentation `6148a9c` livrée le 20/08, **première mesure attendue à la nuit du 21/08** — relire `dream_runs.model` de la phase extract au matin. Sujet modèle fermé par le focus rev 214 : ne pas ré-instruire |
| **Q6** | `76e11c9f` | « Signature **S12** : colonne `thinking_tokens INTEGER NULL` dans la tête **050 déjà formée** (trio d'`ADD COLUMN` nullable), ou renoncement écrit versé au dossier ? » | (1) recommandé : worker code **S** — surcoût nul dans un lot déjà formé, alors que le renoncement laisse une mesure fausse en circulation sur **~29 % des lignes de nuit** (235/823 depuis le 07/08) |

**Décisions secondaires attachées aux autres catégories** (charge de signature complète, à ne pas oublier) : `a7d85a85` (A/B/C, cf. §5) · `2e921e14` (réaligner `current_phase` via `brain_update_project_focus`, **opérateur seul**) · `d04dc588` (les 5 points de SPEC §6) · `c60d023d` (superséder la décision `24495130`) · `b93e32be` (forme (i)/(ii)/(iii) après chiffrage) · `d45689a3` (GO mutation prod) · `74ab1931` L1 (geste clé DR) · `a3fa6696` (1) (geste hors dépôt) · `2547b4a2` (reject non horodaté acceptable ?) · `af3b58dd` (b) et (c) (dépréciation / alias).

---

## 5. À RE-CADRER — 12 tickets (le corps ment ; un message de fil, aucune transition)

| ticket | ce qui est mort dans le corps | re-cadrage proposé | effort / exécutant |
|--------|-------------------------------|--------------------|--------------------|
| `94738f25` **(tracker mission #1)** | « Trois questions attendent l'opérateur » (0 attend), « Phase 0 à 5/6 » (6/6 : `SPEC-pool-brouillons.md`, 216 l.), bandeau « aucune migration ne sera écrite » (mort avec S2-S5) | **Devient le tracker UNIQUE de la livraison 046** (M-A+M-G + auto-ouverture fail-open + claim + fermeture d'inactivité, flag FERMÉ) et rien d'autre. Se ferme quand la 046 est en prod, `alembic_current` mesuré à 046, E2E + canary HTTP v4 verts. État mesuré à rejouer, jamais recopier : prod=045, **zéro blob 046 dans tout l'historique**, `feat/session-nature-046` à diff nul | S / orchestrateur ops ; **la livraison L est déjà attribuée au worker — ne pas la ré-attribuer** |
| `a7d85a85` | « robinet fermé, tri fini et non récurrent » (**faux** : 110 fresh/187 archived contre 93/197, +10 lignes en 5 j, l'agent écrit ~2/jour) ; « REORG flag-only » (**faux** : WET depuis le 2026-08-18, `bc5610e6`, projet dans le pool) | La question n'est plus « que fait-on de 8 snapshots » mais « quelle politique pour un projet qui produit 2 entrées opérationnelles par jour ». Ce qui exempte le résidu, ce sont les **deux gardes** : 87/110 bloqués par `access_count>5`, 25/110 seulement couverts par l'allowlist de forme. Puis **décision opérateur** (A) projet non-capitalisant / (B) étendre l'allowlist + assouplir le garde-fou / **(C) fermer la source côté red-shrik** — recommandation (C) puis (B). **AUCUNE suppression sans signature** (précédent `a9c75b94`) | S (message) ; exécution M pour (C), S pour (B) / worker code |
| `530d796a` | (d) digest-pin **CLOS** (`docker-compose.yml:70`, sha256:c1ddeb6d) ; (b)(c) délégués à `d45689a3` ; (a) n'est pas « en attente de coordination » | **Réduire le ticket au seul point (a) : AUTH — absent du code**, zéro occurrence de auth/bearer/token/allowlist dans les 449 lignes du shim versionné. Implémentation ensuite : bearer statique fichier 0600 sur le patron `codex_gateway/auth.py`, en TDD, **mode header optionnel** ; **ne pas armer côté serveur avant que `9ef5c69d` ait refait son client** (sinon les 7 pipelines Dagster horaires tombent) | S (message) puis M / worker code |
| `9ef5c69d` | **Le jalon « livré » du 2026-08-01 est PERDU** : `EMBEDDING_TOKEN_FILE` n'existe dans aucun fichier, aucune branche, et `main` (08bc362, 2026-08-17) est postérieur de 16 jours | Poster le **constat de perte** (c'est le livrable du triage), remettre à zéro, et **imposer la séquence : derrière `530d796a`(a)**. Leçon à porter hors du ticket : un jalon dans un worktree sans branche poussée ne survit pas — **exiger un SHA sur une ref nommée, pas un rapport de tests** | S (message) puis M (client) + M (cutover coordonné) / worker code auto-discord puis ops |
| `8eaefe36` | Prémisse centrale tombée : « aucune nouvelle version de contrat » — le mint v5 est **signé MINIMAL** (`567f6298`) et **exclut** cette dette du chemin critique | Baseline de référence = **v5**, plus v4/037. Mesures du jour : head 045, 9 séquences, 55 triggers non-internes, couverture séquences **0** dans v4, contraintes héritées vérifiées par **NOM seul** (`brain-v42-v4.sql:1258` et `:1333-1338`) là où les tables v4 le sont par définition (:621, 656, 777). **Split en 4 tickets** (contraintes héritées / séquences / fingerprint des fonctions de trigger / ACL-propriétaires), sur le modèle de `23962510`. **La fermeture de ce ticket après le split exige une contre-vérification sceptique** (cf. angles morts) | S (message + 4 créations) / orchestrateur ops ; L cumulé à l'exécution |
| `2e921e14` | « stock jamais trié » **FAIT** (582→88, 550 rejets datés `operator-decision-2026-08-16`) ; « le nocturne ne sait pas dire archive » **FAUX** depuis le 2026-08-20 (proposition `archive` sur `red`) ; « plus aucune done ni deployed » **PÉRIMÉ** (done 1, deployed 2) | Réduire à 3 points : (1) `current_phase` = « production — all milestones done, deployed » **au-dessus de 29 features en cours** → décision opérateur seul ; (2) **test d'ÉMISSION** de l'op archive (`test_roadmap_curate_apply.py` ne teste que l'application) ; (3) provenance du balayage du 2026-08-14 et son autorisation d'archiver 28 features épinglées, à acter perdue si introuvable. **Le killswitch reste enabled+dry : ne rien flipper ici** | S (message) ; (2) M worker code ; (3) S ops |
| `d04dc588` | 3 points à **ne plus implémenter tels quels** : snapshot sur `brain_sessions` → table append-only ; CAS `expected_checkpoint_revision` → `UNIQUE(session_id, seq)` + `ON CONFLICT` ; « checkpoint rafraîchit heartbeat » → **le checkpoint n'écrit JAMAIS `last_heartbeat_at`**, 2 tests d'absence d'effet le gardent | Gate documentaire **levé** (`SPEC-checkpoint.md`, 317 l.). Reste dû ici : l'approbation produit de **livraison** + 4 points non signés de SPEC §6 (collision de contenu, politique d'appel, seuil B7 à répondre **avec Q5**, borne des checkpoints rendus par `resume`). **La livraison n'appartient plus à ce ticket** : M-C, derrière M-A+M-G, suivie par `94738f25` | S (message) ; signature S opérateur ; code **L** séquencé après M-A+M-G |
| `87ac8b7a` | Symptôme réparé **à la main** (045=045) ; et la piste recommandée est **structurellement morte** : `CLAUDE.md` n'est plus suivi par git (`.gitignore:74`), `test_documentation_contract.py:32/1817` fait s'évaporer l'assertion en silence sur un clone neuf | La réconciliation ne peut plus vivre que **côté briefing**, en lisant l'arbre via `project_contexts.local_path` — **un pouvoir neuf**, de la même famille que le subprocess déjà mis en non-objectif : c'est la question de conception qui remplace celle des slots. **Fermer** la question « slots nommés ? » : la réponse signée est M-D `project_focus_history` + `brain_focus_history` + `focus_diff`. Corriger : `7adeddf2` fermé, 33 tickets ouverts et non 27 | S (message) ; **rien à exécuter avant l'étape C5**, puis M / worker code |
| `259cfbe5` | AC2 **livré** (`session_tools.py:70-76`) ; la « preuve vivante » `7adeddf2` est **morte le jour même** (closed 2026-08-16 19:30) ; `191b2dba` porte son échéance **dans son titre** et est visible au rang 4 sans colonne | Chiffres remesurés : 36 ouverts → 31 tus ; pinned 45/193 et 88/896. **Le dommage doit être re-démontré sur un cas vivant** avant d'ouvrir une tête de migration dans un couloir à 8 rendez-vous. Comparer honnêtement **convention de titre + compteur de tus** (coût zéro, déjà en production de fait) à la colonne `deadline`. Volet colonne = lot C12 avec `b68b9692` | S (message) ; volet surfaçage S ici |
| `7ffe0e8a` | Deux mesures mortes : sweep **ARMÉ ET WET** depuis le 2026-08-19, 21 abandons `auto_stale_7d` ; 9 open / 5 stale / **0 >7 j** contre 23/21/8 | Le mécanisme a **changé d'objet** : auto-heartbeat → `last_observed_at` (D5, flag `BRAIN_SESSION_OBSERVED_ACTIVITY_ENABLED`, livré fermé) ; auto-capture → capture préparée + pool de brouillons (`fc8651ad`, M-E). **Ce ticket devient le porteur de l'ARMEMENT de ces deux flags, pas de leur conception.** Reste intact : 65/317 sessions terminées capturent (20,5 %) | S (message) ; armement = décision opérateur **S**, après le rendez-vous prod 046 |
| `863ff2ca` | Point 2 **RÉFUTÉ comme défaut** par `4890a475` (pour Codex, `conversation.id` **EST** le `session_id`, mesuré strictement égal) ; point 1 **dormant sans producteur** (Claude Code n'exporte aucun OTLP : 0 ligne `/v1/logs/claude` sur 7 j) | Point 2 → simple **incohérence de nommage** (`:143-144` dit « session UUID », `:375` passe `conversation_id`) : une ligne de docstring + un test qui épingle l'égalité — **worker code, S, exécutable tout de suite**. Point 1 → condition de réouverture **écrite** : armer l'OTLP Claude. Point 3 → **seul point vivant, reformulé** : depuis le 2026-08-10 la moitié brain est clé par **TRANSPORT** (`:528-529`), les 64 places sont disputées par le **taux de reconnexion** → à trancher **avec l'option (a) de `40dbfeb1`** (purge sur `DELETE /mcp`), pas séparément | S (message) + S (reliquat) / worker code |
| `c60d023d` | **Le titre est périmé** : `046` est prise par la tête sessions (`c5160259`, `9d22bc6a`, focus rev 214) ; le lot est C7, **proposé en 052, dernier** du couloir ; le gate « M-A attend la 046 embedding » est **LEVÉ** | Le vrai coût n'est pas le DDL (**NO-OP à 1536 partout**) mais le **harnais de test absent** : planificateur pur `_plan(rows, configured_dim) -> list[str]` testable en unitaire, ou fixture d'intégration à seconde base. 9 colonnes et 5 littéraux re-mesurés inchangés. **Puis superséder la décision `24495130`** (= S14), qui ordonne encore « la 046 dimension en second lot » | S (message) + S (supersession) / orchestrateur ops + opérateur |

---

## COMPTE FINAL

> **4 à fermer / 1 à fusionner (+1 fusion refusée) / 16 actionnables / 7 bloqués opérateur / 12 à re-cadrer.**
>
> **Le backlog passe de 41 à 38 tickets vivants** (−3 fermetures dans le périmètre vivant : `fbacb250`, `2bd14b24`, `d30cf6e5`), plus `40dbfeb1` qui passe de `resolved` à `closed` (hors des 41).
> — **36** si l'opérateur tranche **(b)** en Q2 (annulation des deux sortants red-codex restants).
> — **44** une fois exécutés les splits et successeurs proposés : **+7 créations** (4 du split `8eaefe36`, successeur `test-coverage` de `0850cc37`, successeur « transport non purgé sur `DELETE /mcp` » de `40dbfeb1`, extraction UI red-codex de `52d6b319`) **−1** (`8eaefe36` refermé après son split). `23962510` (suite v5) **existe déjà** — ne pas le recréer.
>
> **Le triage ne purge pas le carnet, il convertit du brouillard en travail nommé** : 12 corps de tickets mentent aujourd'hui et 6 questions bloquent 7 tickets. C'est le chiffre honnête.

---

## ANGLES MORTS ET CAVEATS

1. **Deux fermetures proposées par les juges n'ont PAS été contre-vérifiées** et sont donc **interdites de signature en l'état** (règle 1 de la mission) : `0850cc37` (resolve) et `8eaefe36` (resolve après split). Elles sont rangées en actionnable/recadrage avec « contre-vérification sceptique » en tête d'action.
2. **3 tickets vivants du périmètre n'ont pas été triés** : `385dcc72` et `079d4422` (acked, dogfood du 2026-07-04, **`closed_at` posé mais statut `acked`** — incohérence de lifecycle à regarder pour elle-même), et `23962510` (créé le 2026-08-21, suite v5 `content_updated_at_041`).
3. **`40dbfeb1` était déjà `resolved`** depuis le 2026-08-11 : le geste F4 est `resolved`→`closed`, pas `open`→`resolved`. Vérifié en base ce jour.
4. **Le message de fermeture de `fbacb250` contenait une chronologie fausse** (fermeture du 08-13 au lieu du 08-02 ; « une autre route » alors que le travail est passé **par cette même surface**). Les 3 corrections sont appliquées ci-dessus — **ne pas signer une version antérieure**, elle graverait une causalité inversée sur le seul artefact conservé.
5. **Le doublon `b68b9692`→`259cfbe5` a été REFUTÉ**, et le juge du lot 4 proposait la fusion **inverse**. Les deux directions s'annulent : décision finale = **aucune fusion**, liaison + partage de périmètre. Risque résiduel non mesuré : le titre de `259cfbe5` ne parle que du briefing — un futur triage qui livre la colonne `deadline` et ferme le ticket **droperait le volet readiness**.
6. **Cohérence de famille red-codex** : fermer `fbacb250` seul laisse deux sortants ouverts sur la même surface et le même silence amont. La justification est écrite (consommateur disparu vs contrats dormants fail-closed) mais **l'opérateur doit la valider explicitement** en répondant Q2.
7. **Toutes les mesures sont des instantanés** : le 22/25 de l'attestation v5, les 40 refs `/codex` échantillonnées sur 448, les `ss -tn` sur `:8003`, le cockpit à 1-2 lignes. **Les rejouer, jamais les recopier** — ce fil a déjà vu sa recette mentir une fois.
8. **Tout SHA antérieur au 2026-08-17 est mort** : l'historique a été réécrit à la racine `f4b83c7` (« feat: initial public release — brain-v42 0.2.0 »). `3ec7aaf1`, `7508546e`, `90d50f4`, `c5fd9e1` ne résolvent plus. **Citer fichier:ligne, pas des hash** — au moins 3 corps de tickets sont concernés.
9. **`docs/design/` est NON TRACKÉ** (`git status: ?? docs/design/`), de même que `docs/upstream/2026-08-19-b8-session-join-rejeu.md`. Six dispositions s'appuient sur ces documents. Seules les **décisions en base** (`c5160259`, `9d22bc6a`, `567f6298`, `1bc19052`, `ee271fbf`, `fc8651ad`, `4890a475`, `bc5610e6`) sont durables. Publier le répertoire est une décision opérateur — **elle n'est un préalable à aucune des 4 fermetures**.
10. **Aucun outil MCP n'édite un corps de ticket** (`cabb7503`, A13) : **tous** les recadrages de la section 5 passeront par des messages de fil, donc **12 corps périmés resteront lisibles et menteurs** en tête de ticket. C'est le mode de panne que ce triage vient de documenter trois fois (`52d6b319` head-037, `eb067b57` 24/25, `c60d023d` « 046 »).
11. **Contraintes de couloir à respecter absolument** : jamais deux têtes Alembic en vol (focus rev 214) → `2547b4a2` lot B, `b93e32be` (ii)/(iii), `b68b9692`, `259cfbe5`, `76e11c9f`, `c60d023d` sont **tous interdits** tant que la 046 est en vol. Et **rien de lourd à deux heures d'une nuit à dix projets** (`1c40c36a`, `d45689a3`).
12. **Deux worktrees workers actifs** (`.claude/worktrees/`) ignorés comme demandé ; toutes les lectures de code portent sur l'arbre principal à `14c0385`, prod mesurée à `045`.
