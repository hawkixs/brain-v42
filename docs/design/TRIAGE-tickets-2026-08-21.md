# FINAL DISPOSITION TABLE — brain-v42 ticket triage, 2026-08-21

**Scope measured today** (`psql`, main tree at `14c0385`, prod at `045`): **41 live tickets** in the brain-v42 orbit = 36 addressed to `brain-v42` + 5 outgoing from `brain-v42` (`9ef5c69d`→auto-discord, `fbacb250`/`e45976c0`/`a1090d46`→red-codex, `89140780`→red-shrik). **+1 object** in `resolved` awaiting confirmation (`40dbfeb1`). **39 tickets triaged**, 3 untriaged (see blind spots).

Counting rule: one primary disposition per ticket; secondary actions are annotated on the line. `verdict_juge` kept for audit.

---

## 1. TO CLOSE — 4 tickets (only verdicts CONFIRMED by a skeptic)

| # | ticket | move | one-line evidence |
|---|--------|-------|---------------------|
| F1 | `fbacb250` Codex Desktop provisioning (→red-codex) | **CANCEL** (never `resolve`) — operator, S | Consumer vanished: 0 Brain session with `client_key` codex since 2026-08-11 (228 total), 0 file under `~/.codex/sessions` since 2026-08-11 09:58, only live rail = `codex exec` (`scripts/dream/codex_runner.py:1,159`), 0 red-codex message in 20 days. |
| F2 | `2bd14b24` Idea — session lifecycle vs subagents | **RESOLVE** — ops orchestrator, S | FRAMING ticket, framing done and signed 4× in the database: `1bc19052`, `ee271fbf` (2026-08-19), `c5160259` (2026-08-20), `567f6298` (2026-08-21); `DOSSIER.md:486` and `:503` name it. |
| F3 | `d30cf6e5` Anchor for the PROJECTS+SESSIONS overhaul | **RESOLVE** (+ merge into `94738f25`) — ops orchestrator, S | Mandate consumed: `docs/PROJECTS_SYSTEM.md` (234 lines, `e04df96`) literally reproduces its 4 building blocks, and the "DO NOT START without framing" gate is lifted by `c5160259`/`9d22bc6a`/`567f6298` (all `active`). |
| F4 | `40dbfeb1` Brain tab red-monitor (→red-monitor) | **CONFIRM** `resolved`→`closed` — ops orchestrator, S | Already `resolved` since 2026-08-11; the 4 requests verified in the **served bundle** (`assets/Brain-CYLdNcGV.js`, 11 corrective strings) and not just in source; `red-server` MainPID=996570, `NRestarts=0` since deployment. |

### F1 — closing message (3 mandatory corrections applied)

> Closed on 2026-08-21 for **CONSUMER DISAPPEARANCE, not for a fix** — the defect was never replayed. `80e92492`, the only work this incident was blocking, has been closed since **2026-08-02 00:05** (2026-08-13 is only its `updated_at`) — and it was delivered **THROUGH THIS VERY SURFACE**, after transient recovery: threads `019fbfa3` (implementation), `019fbfad` then `019fbfb8` (reviews), `019fbfbd` (integration), SHA `8c436aa7`. The defect is therefore **intermittent, not fixed**. No Brain session with `client_key` codex since 2026-08-11 (228 total); corroborated outside Brain: no file written under `~/.codex/sessions` since 2026-08-11 09:58. The only codex rail still alive is `codex exec` (`scripts/dream/codex_runner.py`), which uses neither `create_thread`, nor `list_projects`, nor `set_thread_archived` — no caller of these primitives anywhere in the tree. No red-codex message on this ticket in 20 days; the last red-codex message across all tickets dates to 2026-08-01 16:46, i.e. 4 h 37 **before** this ticket was created: upstream never read it. **We're closing for absence of a consumer, not for withdrawal of the surface**: the codex app-server is still running (PID 2999, unit `~/.config/systemd/user/codex-app-server.service`, codex-cli 0.147.0) — resuming this surface tomorrow would require no re-provisioning. To keep if the subject comes back: bounded timeout and terminal result on every task operation, status observable by `clientThreadId`, idempotent cancel/reconcile, and the terminal loss on the second round (`CreateProcess ENOENT`, reproduced on `019fc035…` and `019fc042…`) with its measured workaround. The archiving/worktree hygiene angle stays carried by `a1090d46`. Reopen rather than recreate.

### F2 — closing message

> **RESOLVED as a FRAMING ticket, on 2026-08-21.** The three questions it raised are settled and signed: (1) end ritual → path (a), **two session natures**, `agent` traced auto-closed without a ritual and `operator` with a ritual (settled on 2026-08-19); (2) subagent → **INHERITANCE** from the carrier's session, settled **by measurement** rather than by arbitration — none of the three headers distinguish a subagent from its carrier; (3) automatic lifecycle → auto-open on the (project, CONNECTION) key **HTTP only — no automatic session in stdio** (`c5160259` b) and `closed_inactive` inactivity closure by the nightly sweep. Durable signatures in the database, independent of any file: `1bc19052` and `ee271fbf` (2026-08-19), `c5160259` (2026-08-20), `567f6298` (2026-08-21: `nature VARCHAR(16) CHECK ('agent','operator')`, `closed_inactive ⇒ nature='agent'` in the terminal CHECK). The false-alive failure mode that opened this ticket is closed empirically: sweep WET since 2026-08-19 (`killswitches.conf:62,65`), 21 `auto_stale_7d` abandonments on 2026-08-20, **zero** session open for more than 7 days today versus 39 collected by hand on 2026-08-06. IMPLEMENTATION is not done and is not carried by this ticket: it lives in migration 046 (M-A+M-G), carrier `94738f25`.

*(Condition lifted: the skeptic established that `1bc19052`/`ee271fbf` carry the arbitration **in the database** — publishing `docs/design/` is NOT a prerequisite for this closure.)*

### F3 — closing message (2 editorial corrections applied)

> **Anchor filled, then superseded.** The starting point this ticket was meant to fix is documented — `docs/PROJECTS_SYSTEM.md` (234 lines, commit `e04df96`, branch `test/phase0-session-anchors`) and the corpus `docs/design/refonte-projets-sessions/` (ADR, PLAN, DOSSIER 645 lines, SEQUENCEMENT, SPEC-checkpoint, SPEC-M-G, SPEC-pool-brouillons, baseline). The framing this ticket required BEFORE any start is signed: `c5160259`, `9d22bc6a`, `567f6298`. **The live project is tracked by `94738f25`**, which carries the current state. The reattached session pains (`2bd14b24`, `d04dc588`, `7ffe0e8a`) live **in the ADR (§ grid B, lines 576-585)**, not in `94738f25`'s thread. The four residuals from thread #2 are folded: `expected_session_indexes` in SEQUENCEMENT lines 63/231, `v4.sql`/`v4-pgrestore.sql` parity and `SESSION_INDEX_DEFINITION_MD5` in SEQUENCEMENT lines 232/235, index decision MEASURED AND CONCLUDED in PLAN line 1432, spike B8 replayed on Claude Code 2.1.234. The ghost stock has dropped back to 9 open / 0 sweepable. No status review to redo: **the DOCUMENTS are authoritative, not this thread.**

*(Non-blocking reservation to escalate: `PLAN:604-616` still reads "three nullable columns … `started_by_actor` created without an index" without an amendment box, contradicting `PLAN:1432`.)*

### F4 — confirmation message

> **CONFIRMED on 2026-08-21, cross-checked on both ends and not taken on faith.** Code: `BrainActivity.jsx` carries the two groups in different columns (:101, :135), the empty group keeps its header (:108-110), "attempt" and never "call" (:32-34, :72), `_process` hidden but counted (:87, :144-146), the header rewritten — join "proven" and "not wired", zero occurrence of "impossible" (:24-28) —, the actor/session-unproven warning (:174-177); the **global sort before filtering** is at `brainPresentation.js:145-157` (`visible.sort(byRecency)`, `POSITIVE_INFINITY` for unmeasured recency, `localeCompare` as tiebreak). The replacement requested in message 1 has been adopted: `client?.kind === 'session'` (:84), pinned by `brainPresentation.test.js:35` and `Brain.test.jsx:365-371`. **Runtime, ten days after your deployment**: `red-server.service` active, MainPID 996570 and `ActiveEnterTimestamp` 2026-08-11 21:23:24 CEST — the PID and time announced in message 4 —, `NRestarts=0`, :8081 → 200 serving `assets/index--Tui5Tgp.js`, corrective strings verified in the chunk `assets/Brain-CYLdNcGV.js`. Point 1(c) left aside: accepted, your reason is the right one. **YOUR MESSAGE 5, TAKEN AND ANSWERED**: you are right, a transport is not a caller and no presentation catches it. We're taking option (a) — remove the transport from the live registry on `DELETE /mcp` — because it requires no contract change on your side. **It is NOT done**: `client_activity.py` today has no path that removes a transport before its 600 s TTL (`:43`), and no transport-end receiver exists. We're opening it as a separate brain-v42 ticket rather than reopening this one, which is not about presentation. Your point 6 on red-lab is already our ticket `a3fa6696` (cause intact: `brain_client.py:30`, `Authorization` only; service simply stopped, hence your empty `unknown` bucket): we're carrying it, don't open it on your side.

---

## 2. TO MERGE — 1 confirmed merge (+1 REFUSED merge to record)

| direction | status | move |
|-----------|--------|-------|
| `d30cf6e5` → **`94738f25`** | **CONFIRMED** by the skeptic (full coverage verified: the 3 pointers and "today's lived experience" are absorbed by `DOSSIER.md:79,82,220,237-238,276-285` and `ADR:576-585,1067,1809`) | The merge is executed VIA F3's `resolve` + a mirror note on `94738f25` (**neither references the other in the database today** — the resolution message creates the only link) |
| `b68b9692` → `259cfbe5` | **REFUSED** (skeptic verdict REFUTED) | **No merge.** Reason: the target only covers "deadline + briefing cap"; 4 of the 6 requests of the would-be duplicate (readiness, dependencies, reason/evidence, non-authoritative direct gate) are absent from it, and `259cfbe5` **argues AGAINST** a manually-set rank ("the solution NOT to adopt"). The judge for batch 4 proposed the **reverse** merge: the two directions cancel out. |

**Replacement disposition for the pair `b68b9692`/`259cfbe5`** (2 linking messages, ops orchestrator, S): `b68b9692` **cedes** its "priority" angle to `259cfbe5`'s deadline track and **keeps** readiness + dependencies + reason/evidence + direct gate; `259cfbe5` carries the **single** `ADD COLUMN` on `tickets` (batch C12) and must be re-reviewed with `b68b9692`'s scope in front of it. Numeric ratification of acceptance criterion 3: `features.pinned` = 45/193 (brain-v42), 88/896 (global), 2026-08-21.

---

## 3. ACTIONABLE — 16 tickets, prioritized (current mission first: prod 046 → git clean/e2e → hardening → tech debt)

| rank | ticket | headline action | effort | executor | gate |
|------|--------|----------------|--------|-----------|------|
| **A1** | `eb067b57` Recovery v5 | Mint `ops/recovery/brain-v42-v5.{json,sql,-pgrestore.sql}` — **25 checks, `alembic_head` DERIVED** (previously `test_alembic_env.py:254-259`), re-minted AFTER 046; `test_recovery_contract_v5.py` (byte-exact freeze + SHA-256 of v4 assets); fix the contradictory attestation gates of `PLAN_INDEX_REPAIR_RUNBOOK.md` (lines 63/66/583 say 25/25, lines 154-155 say 24/25) | **L** | code worker (worktree `feat/session-nature-046`) | **critical path 046**; DO NOT apply the "lines 1789/1791" recipe from the 08-06 thread, obsolete since S1; **replay** the attestation in `BEGIN READ ONLY` — the 22/25 from 08-20 is stale |
| **A2** | `74ab1931` DR v3 — **batch L1 only** | **Move `~/.config/brain-v42/dr/PRIVATE-KEY-A-DEPLACER-HORS-DE-CET-HOTE.asc` off this host and delete the original** (3478 bytes, mtime 08-16 unchanged) | **S** | **operator** | none — do it right away; L2/L3/L4 below |
| **A3** | `4a6fe67e` Dedup features | `systemctl --user restart brain-metrics` + verify the new PID postdates `64caf89` — **this move, and only this one, arms the `pinned` guard in prod** (live PID 1282863, started 2026-08-14, two days BEFORE the guard) | **S** | ops orchestrator | rearms the 6 h countdown, this time on guarded code; post the reframe "defect 1 fixed, not armed; defects 2 and 3 open" |
| **A4** | `a1090d46` **git-clean spin-off** | Measure the **448 `refs/codex/snapshots/*` refs** in full (`for r in $(git for-each-ref --format='%(refname)' refs/codex); do git merge-base $r main >/dev/null 2>&1 \|\| echo $r; done \| wc -l`) and set their fate **inside the signed CLEAN GIT step** — 40/40 sampled with no merge-base, while all 4 branches have one: **the move as written ("branch -D the 10 branches with no merge-base") would not touch the real carrier of the pre-rewrite lineage** | **S** | ops orchestrator | **BEFORE** the CLEAN GIT step of focus rev 214 |
| **A5** | `9e1a2b39` CAPABILITY_ENFORCEMENT | (1) Settle `Environment=` vs `EnvironmentFile=` precedence **empirically** with the probe already present at `tests/integration/test_dream_systemd_install.sh:385-420`; (2) set `Environment=BRAIN_DREAM_CAPABILITY_ENFORCEMENT=true` at the end of `killswitches.conf` with a comment saying that **the source of truth remains `mcp-token.env` (`token.conf:5`, sorts AFTER, wins)** and that this line is a **review mirror**, not an arming; (3) `daemon-reload` | **S** (~15 min) | ops orchestrator | no restart; effect on the next night; **never compose with `echo`, don't touch `mcp-token.env`** |
| **A6** | `0850cc37` Postgres CI | **Mandatory skeptic cross-check** (see blind spots) then `resolve` + open the narrow successor "`test-coverage` measures 85.29% with no 55 tests backed by a database": copy `services.postgres` + `BRAIN_V42_TEST_DB_URL` + `alembic upgrade head` into `test-coverage`, and **INVERT** the witness `test_github_workflows.py:225-239` instead of deleting it | **S** then **S** | ops orchestrator → code worker | the body cannot be re-titled (`cabb7503`): "actionable and false" is the worst combination |
| **A7** | `191b2dba` 2026-09-03 review | Post the **guard-rail**: step 3 ("if zero rows: close") is a trap — the `run_date > '2026-08-20'` window returns zero rows **when no night has run in it**, which is the case today. Require a denominator ≥ 7 nights of extract. Log the morning-of-08-21 result here as a baseline | **S** | ops orchestrator | **close nothing before 2026-09-03** |
| **A8** | `d45689a3` Embedding shim rollout | (1) `docker tag brain_v42-embedding-shim:latest …:pre-sec2-20260706` **before** any rebuild (the rebuild overwrites the only July copy); (2) window outside dream night and outside 046; (3) override `ports:["0.0.0.0:8003:8003"]` to deliver only the **limits**, then remove the override for the **bind**; (4) canary `/embed` + `/rerank` + a real `brain_search`; (5) 24 h of monitoring | **M** | ops orchestrator | **explicit operator GO** (out-of-band prod mutation: if the shim breaks, `brain_search` and rerank go down — skill `debug-brain-v42-embedding-500`) |
| **A9** | `b93e32be` access_log | (a) **Finally cost out the flow** over 24 h (two readings of `sum(access_count)` on the six tables), to produce the storage cost this ticket has been asking for since it opened. Fresh finding to log in the thread: `pg_access_log.purge_old(days=30)` (:175-183) **already exists** and has only its unit test as a caller | **S** | ops orchestrator | **strictly growing** debt: 754/718 → **837/807** in 5 days; arbitration (b) = operator, **no write before the 046 milestone** |
| **A10** | `1c40c36a` ActivityReporter | One commit: **coalescing** emitter (dict aggregated on (actor, session, transport), pump task, `MAX_OBSERVATIONS=64`, `calls: N`) — closes A and B together without touching the sidecar | **M** | code worker | fire-and-forget contract unchanged; the anti-tautology probe must stay green; **DO NOT ship two hours before a ten-project night**. Fallback **S**: batch only B |
| **A11** | `a3fa6696` red-lab X-Brain-Agent | (1) **outside this repo, service stopped**: add `"X-Brain-Agent": self.author_project` to `brain_client.py:30` then redeploy `/opt/red-lab/current`; (2) **inside this repo**: fix `docs/superpowers/specs/2026-08-06-live-client-activity-design.md:245` ("basename of the **LAUNCH** directory, not a project name") | **S** + **S** | operator (1) / code worker (2) | fresh finding for the thread: the ADR creates `started_by_actor VARCHAR(64)` (line 692) — from Phase 1 onward a client with no header writes `unknown` into a **durable column** |
| **A12** | `2547b4a2` MCP curation apply/reject | Reframe (the stock HAS MOVED: 582→88 proposed, **+543 rejections, 0 applies**, written **in raw SQL outside the surface**) then **batch A**: MCP tools `apply`/`reject` scoped by `features.project_key` join, `project_key` required, mutation on `proposed` only, no "apply all" | **S** + **M** | ops orchestrator → code worker | **batch A with no migration → playable during 046**; **batch B** (rejection audit columns) requires a head → **forbidden** while 046 is in flight. Operator confirmation required: is an un-timestamped reject acceptable in the meantime? |
| **A13** | `cabb7503` Ticket editing | `brain_ticket_edit(ticket_id, author_project, title, body, reason)`, restricted to participants, which **(a) does NOT write `updated_at`** (no trigger forces it — measured) and **(b) systematically logs the old text to the thread** | **M** | code worker | **no migration**; submit (a) and (b) to the thread as answers to the body's 2 questions before executing. Lower-commitment variant: title + "STALE — see thread" banner, immutable body |
| **A14** | `af3b58dd` MCP hygiene | (a) `brain_set_project_context` → single Pydantic model (16 flat params) + compat test with 16 kwargs; (b) `brain_propose_adr` → extract the dream-promotion path so the XOR (lines 440-443) becomes a schema property; (c) `brain_list_adrs` | **M**+**M**+**S** | code worker | (b) requires a deprecation decision (dream prompts call it); (c) requires an alias-vs-removal decision — note that under the `compact` profile the duplication is only visible from `brain_find_tool`, and fix the thread sentence naming `list_adrs` as a tool |
| **A15** | `89140780` run_qa (→red-shrik) | Make the URL and model explicit and configurable, fix the docstring at line 4 (vanished `red-data/nomic` topology), add an anti-hardcoded-IP test. **Drop from scope** the authentication angle: the endpoint `192.168.1.11:8003` is **unreachable** (curl timeout rc 124) and the dev-pc runs Windows 11 | **S** | red-shrik code worker | no prior decision; the question "remove / dormant / repoint with a parity baseline" stands **without** waiting for it, recommended default = dormant |
| **A16** | `b68b9692` Readiness (former duplicate, **REFUTED**) | Rescope: readiness + dependencies + reason/evidence + direct gate before launch (inventory or **non-authoritative** sub-agent); cede "priority" to `259cfbe5`; post the 2 linking messages | **S** (linking) then **M** | ops orchestrator → code worker | **back to actionable because the duplicate was REFUTED**; migration behind 046 ("never two heads in flight") |
| **A17** | `b3331691` Gateway preflight | Extend `_GATEWAY_CONTRACT_PROOF` (`rotate_codex_gateway_credentials.py:178`) to the **3 missing clauses** (`security_barrier`, 2 triggers) by reading the same predicates as `composition.py:61-79`; **write the layering arbitration** (duplication + agreement test, or SQL in a shared data file); link `_CODEX_GATEWAY_CONTRACT` to `CONTRACT_COLUMNS`; remove the false comment `test_rotate_…:1241` | **M** | code worker | **before any gateway rollout**, so the first rotation is guarded; zero urgency today (no container, `curl :9211/ready` rc 7) |

*(A17 carries rank 17: 16 tickets + spin-off A4, which is not a ticket. Total actionable tickets = **16**.)*

**Attached deferred batches** (not to lose track of): `74ab1931` L2 automated off-host with remote retention — **code worker in red-backup, L**, hard prerequisite **S** = measure the dev-pc's uptime rate over 7 days; L3 Neo4j rebuild drill — code worker, **M**; L4 propagate v5 into `red-backup/config/recovery/` and decouple the v1/v2 `Literal`s of `src/backup/recovery_contract.py:213` — ops orchestrator, **M**, **after** A1.

---

## 4. BLOCKED ON OPERATOR — 7 tickets, 6 questions (one sentence each)

| # | ticket(s) | exact question | after signature |
|---|-----------|-----------------|-----------------|
| **Q1** | `52d6b319` | "Do you authorize me to install the privileged helper (`sudo ./deploy/install-brain-shrik-env-control.sh`, present but **never installed** in 20 days) or to set the bounded sudoers rule, without which the `:9211` gateway cutover stays dead?" | ops **M**: coordinated rotation + `docker compose --profile codex-gateway up -d` + readiness/9-view/mutation/409/scoping proof, old credentials refused. In parallel, without waiting: code worker **S** — fix the title (`:9210`→`:9211`) and purge the head-037 claims; **extract the red-codex UI catch-up into a clean ticket** (never done, msg 12 and 32) |
| **Q2** | `e45976c0` **+** `a1090d46` (single question) | "red-codex is measured inert since 2026-08-05 (`project_contexts.updated_at`=2026-08-01, `local_path` empty, 0 thread message across 4 open tickets): (a) staff it, (b) declare 'UNAVAILABLE is permanent' and cancel the two remaining outgoing tickets, or (c) **dated hibernation** with a review date — which one?" | Recommendation **(c)** on the signed poison-pill template (`74bf3e6f`): dated review + monitoring request. **Family consistency to explicitly validate**: `fbacb250` falls alone because its consumer disappeared, whereas these two carry contracts that are **still valid and fail-closed** (routing/cost forbidden outside VERIFIED; criterion (2) of `a1090d46` de facto held by 448 refs) — nothing breaks while they sleep |
| **Q3** | `ac3166d9` | "Does this ticket move to `resolve_pending` (local part delivered, the remainder is host-owned and belongs to `e45976c0`) or to `cancel` (the consumer disappeared)?" | **S**, no line of code on the brain-v42 side. Re-measured against codex-cli 0.147.0: `AttestationGenerateParams` = `{}`, `AttestationGenerateResponse` = opaque token, `Thread` with neither `model` nor `effort`. **Either way, remove `in_progress`** — it describes waiting on a third party |
| **Q4** | `d9a968f7` | "Unify `FeatureLinker` on `ClusterGuard` (A — closes the inconsistency, but touches a path **quiescent for 11 days** carrying 6% of links, with no symptom demanding it) or embrace the documented fallback (B — a test naming the divergence, WARN instead of debug, and the `max_links` cap that `_do_link` **has none of at all**)?" | **(B) recommended**: code worker **S**, TDD, no migration, playable during 046. (A) = **M**, not to be launched in the 046 lane. Arbitration numbers ready: 56 multiple reattachments / 3784 (1.5%), none in 30 days; 3623 guard links vs 232 raw |
| **Q5** | `51455b48` | "Status quo on `DEFAULT_MODEL` dead with 410 (cost = one 410 round trip per run + a warning line, fallback measured) or promote `meta/llama-3.3-70b-instruct` to primary, accepting that no canary against the real extraction prompt will have run?" | (b) = code worker **S** (one constant + one test). Meanwhile, in the thread: instrumentation `6148a9c` shipped on 08-20, **first measurement expected the night of 08-21** — re-read `dream_runs.model` for the extract phase in the morning. Model topic closed by focus rev 214: do not re-litigate |
| **Q6** | `76e11c9f` | "**S12** signature: column `thinking_tokens INTEGER NULL` in the **already-formed head 050** (nullable `ADD COLUMN` trio), or a written renunciation filed to the record?" | (1) recommended: code worker **S** — zero extra cost in an already-formed batch, whereas the renunciation leaves a false measurement circulating on **~29% of night rows** (235/823 since 07-08) |

**Secondary decisions attached to other categories** (full signature load, not to forget): `a7d85a85` (A/B/C, see §5) · `2e921e14` (realign `current_phase` via `brain_update_project_focus`, **operator only**) · `d04dc588` (the 5 points of SPEC §6) · `c60d023d` (supersede decision `24495130`) · `b93e32be` (form (i)/(ii)/(iii) after costing) · `d45689a3` (prod mutation GO) · `74ab1931` L1 (key DR move) · `a3fa6696` (1) (move outside this repo) · `2547b4a2` (un-timestamped reject acceptable?) · `af3b58dd` (b) and (c) (deprecation / alias).

---

## 5. TO REFRAME — 12 tickets (the body lies; a thread message, no transition)

| ticket | what's dead in the body | proposed reframe | effort / executor |
|--------|-------------------------------|--------------------|--------------------|
| `94738f25` **(mission tracker #1)** | "Three questions await the operator" (0 pending), "Phase 0 at 5/6" (6/6: `SPEC-pool-brouillons.md`, 216 lines), banner "no migration will be written" (dead since S2-S5) | **Becomes the SOLE tracker of the 046 delivery** (M-A+M-G + fail-open auto-open + claim + inactivity closure, flag CLOSED) and nothing else. Closes when 046 is in prod, `alembic_current` measured at 046, E2E + HTTP v4 canary green. State to be re-measured, never copied forward: prod=045, **zero 046 blob anywhere in history**, `feat/session-nature-046` at zero diff | S / ops orchestrator; **the L delivery is already assigned to the worker — do not reassign it** |
| `a7d85a85` | "tap closed, triage done and non-recurring" (**false**: 110 fresh/187 archived vs 93/197, +10 rows in 5 days, the agent writes ~2/day); "REORG flag-only" (**false**: WET since 2026-08-18, `bc5610e6`, project in the pool) | The question is no longer "what do we do with 8 snapshots" but "what policy for a project producing 2 operational entries a day". What exempts the residual is the **two guards**: 87/110 blocked by `access_count>5`, only 25/110 covered by the shape allowlist. Then **operator decision** (A) non-capitalizing project / (B) extend the allowlist + relax the guard-rail / **(C) close the source on the red-shrik side** — recommendation (C) then (B). **NO deletion without a signature** (precedent `a9c75b94`) | S (message); execution M for (C), S for (B) / code worker |
| `530d796a` | (d) digest-pin **CLOSED** (`docker-compose.yml:70`, sha256:c1ddeb6d); (b)(c) delegated to `d45689a3`; (a) is not "awaiting coordination" | **Narrow the ticket down to point (a) only: AUTH — absent from the code**, zero occurrence of auth/bearer/token/allowlist in the 449 lines of the versioned shim. Implementation next: static bearer, 0600 file, on the `codex_gateway/auth.py` pattern, in TDD, **optional header mode**; **do not arm it server-side until `9ef5c69d` has redone its client** (otherwise the 7 hourly Dagster pipelines break) | S (message) then M / code worker |
| `9ef5c69d` | **The 2026-08-01 "delivered" milestone is LOST**: `EMBEDDING_TOKEN_FILE` exists in no file, in no branch, and `main` (08bc362, 2026-08-17) postdates it by 16 days | Post the **loss finding** (this is the triage's deliverable), reset to zero, and **impose the sequence: behind `530d796a`(a)**. Lesson to carry outside the ticket: a milestone in a worktree with no pushed branch does not survive — **require a SHA on a named ref, not a test report** | S (message) then M (client) + M (coordinated cutover) / auto-discord code worker then ops |
| `8eaefe36` | Central premise fallen: "no new contract version" — the v5 mint is **signed MINIMAL** (`567f6298`) and **excludes** this debt from the critical path | Reference baseline = **v5**, plus v4/037. Today's measurements: head 045, 9 sequences, 55 non-internal triggers, sequence coverage **0** in v4, inherited constraints verified by **NAME only** (`brain-v42-v4.sql:1258` and `:1333-1338`) whereas v4 tables have them by definition (:621, 656, 777). **Split into 4 tickets** (inherited constraints / sequences / trigger function fingerprint / ACL-owners), on the `23962510` model. **Closing this ticket after the split requires a skeptic cross-check** (see blind spots) | S (message + 4 creations) / ops orchestrator; cumulative L on execution |
| `2e921e14` | "stock never triaged" **DONE** (582→88, 550 rejections dated `operator-decision-2026-08-16`); "the nightly job can't say archive" **FALSE** since 2026-08-20 (`archive` proposal on `red`); "no more done or deployed" **STALE** (done 1, deployed 2) | Narrow to 3 points: (1) `current_phase` = "production — all milestones done, deployed" **on top of 29 in-progress features** → operator-only decision; (2) **EMISSION test** for the archive op (`test_roadmap_curate_apply.py` only tests application); (3) provenance of the 2026-08-14 sweep and its authorization to archive 28 pinned features, to be recorded as lost if not found. **Killswitch stays enabled+dry: flip nothing here** | S (message); (2) M code worker; (3) S ops |
| `d04dc588` | 3 points **no longer to be implemented as written**: snapshot on `brain_sessions` → append-only table; CAS `expected_checkpoint_revision` → `UNIQUE(session_id, seq)` + `ON CONFLICT`; "checkpoint refreshes heartbeat" → **the checkpoint NEVER writes `last_heartbeat_at`**, 2 no-side-effect tests guard it | Documentary gate **lifted** (`SPEC-checkpoint.md`, 317 lines). Still owed here: the **delivery** product approval + 4 unsigned points from SPEC §6 (content collision, call policy, B7 threshold to answer **with Q5**, checkpoint bound returned by `resume`). **The delivery no longer belongs to this ticket**: M-C, behind M-A+M-G, tracked by `94738f25` | S (message); operator signature S; **L** code sequenced after M-A+M-G |
| `87ac8b7a` | Symptom fixed **by hand** (045=045); and the recommended path is **structurally dead**: `CLAUDE.md` is no longer git-tracked (`.gitignore:74`), `test_documentation_contract.py:32/1817` makes the assertion silently evaporate on a fresh clone | Reconciliation can now only live **on the briefing side**, reading the tree via `project_contexts.local_path` — **a new capability**, of the same family as the subprocess already set as a non-goal: this is the design question replacing the slots one. **Close** the "named slots?" question: the signed answer is M-D `project_focus_history` + `brain_focus_history` + `focus_diff`. Fix: `7adeddf2` closed, 33 open tickets not 27 | S (message); **nothing to execute before step C5**, then M / code worker |
| `259cfbe5` | AC2 **delivered** (`session_tools.py:70-76`); the "living proof" `7adeddf2` **died the same day** (closed 2026-08-16 19:30); `191b2dba` carries its deadline **in its title** and is visible at rank 4 without a column | Numbers re-measured: 36 open → 31 silent. pinned 45/193 and 88/896. **The damage must be re-demonstrated on a live case** before opening a migration head in a lane with 8 appointments. Fairly compare **title convention + silent-ticket counter** (zero cost, already de facto in production) to the `deadline` column. Column angle = batch C12 with `b68b9692` | S (message); surfacing angle S here |
| `7ffe0e8a` | Two dead measurements: sweep **ARMED AND WET** since 2026-08-19, 21 `auto_stale_7d` abandonments; 9 open / 5 stale / **0 >7 days** vs 23/21/8 | The mechanism has **changed target**: auto-heartbeat → `last_observed_at` (D5, flag `BRAIN_SESSION_OBSERVED_ACTIVITY_ENABLED`, shipped closed); auto-capture → prepared capture + draft pool (`fc8651ad`, M-E). **This ticket becomes the carrier of the ARMING of these two flags, not their design.** Still intact: 65/317 completed sessions capture (20.5%) | S (message); arming = operator decision **S**, after the prod 046 milestone |
| `863ff2ca` | Point 2 **REFUTED as a defect** by `4890a475` (for Codex, `conversation.id` **IS** the `session_id`, measured strictly equal); point 1 **dormant with no producer** (Claude Code exports no OTLP: 0 `/v1/logs/claude` row over 7 days) | Point 2 → simple **naming inconsistency** (`:143-144` says "session UUID", `:375` passes `conversation_id`): one docstring line + a test pinning the equality — **code worker, S, executable right now**. Point 1 → **written** reopening condition: arm Claude OTLP. Point 3 → **only live point, reworded**: since 2026-08-10 half the brain is keyed by **TRANSPORT** (`:528-529`), the 64 slots are contested by the **reconnection rate** → to be settled **together with option (a) of `40dbfeb1`** (purge on `DELETE /mcp`), not separately | S (message) + S (residual) / code worker |
| `c60d023d` | **The title is stale**: `046` is taken by the sessions head (`c5160259`, `9d22bc6a`, focus rev 214); the batch is C7, **proposed as 052, last** in the lane; the "M-A awaits the embedding 046" gate is **LIFTED** | The real cost is not the DDL (**NO-OP at 1536 everywhere**) but the **missing test harness**: a pure planner `_plan(rows, configured_dim) -> list[str]` unit-testable, or an integration fixture against a second database. 9 columns and 5 literals re-measured unchanged. **Then supersede decision `24495130`** (= S14), which still mandates "the 046 dimension in a second batch" | S (message) + S (supersession) / ops orchestrator + operator |

---

## FINAL TALLY

> **4 to close / 1 to merge (+1 merge refused) / 16 actionable / 7 blocked on operator / 12 to reframe.**
>
> **The backlog goes from 41 to 38 live tickets** (−3 closures within the live scope: `fbacb250`, `2bd14b24`, `d30cf6e5`), plus `40dbfeb1` moving from `resolved` to `closed` (outside the 41).
> — **36** if the operator picks **(b)** on Q2 (cancelling the two remaining outgoing red-codex tickets).
> — **44** once the proposed splits and successors are executed: **+7 creations** (4 from the `8eaefe36` split, `test-coverage` successor of `0850cc37`, "transport not purged on `DELETE /mcp`" successor of `40dbfeb1`, red-codex UI extraction from `52d6b319`) **−1** (`8eaefe36` re-closed after its split). `23962510` (v5 follow-up) **already exists** — do not recreate it.
>
> **Triage doesn't purge the notebook, it converts fog into named work**: 12 ticket bodies lie today and 6 questions block 7 tickets. That's the honest number.

---

## BLIND SPOTS AND CAVEATS

1. **Two closures proposed by the judges were NOT cross-checked** and are therefore **forbidden to sign as-is** (mission rule 1): `0850cc37` (resolve) and `8eaefe36` (resolve after split). They're filed as actionable/reframe with "skeptic cross-check" as the lead action.
2. **3 live tickets in scope were not triaged**: `385dcc72` and `079d4422` (acked, dogfood from 2026-07-04, **`closed_at` set but status `acked`** — a lifecycle inconsistency worth looking at on its own), and `23962510` (created 2026-08-21, v5 follow-up, `content_updated_at_041`).
3. **`40dbfeb1` was already `resolved`** since 2026-08-11: move F4 is `resolved`→`closed`, not `open`→`resolved`. Verified in the database today.
4. **The `fbacb250` closing message contained a false timeline** (closure on 08-13 instead of 08-02; "a different route" when the work went **through this very surface**). The 3 corrections are applied above — **do not sign an earlier version**, it would engrave a reversed causality onto the only artifact kept.
5. **The `b68b9692`→`259cfbe5` duplicate was REFUTED**, and the judge for batch 4 proposed the **reverse** merge. The two directions cancel out: final decision = **no merge**, linkage + scope sharing. Unmeasured residual risk: `259cfbe5`'s title only talks about the briefing — a future triage that delivers the `deadline` column and closes the ticket **would drop the readiness angle**.
6. **red-codex family consistency**: closing `fbacb250` alone leaves two outgoing tickets open on the same surface and the same upstream silence. The justification is written (vanished consumer vs still-valid fail-closed contracts) but **the operator must explicitly validate it** by answering Q2.
7. **All measurements are snapshots**: the v5 attestation's 22/25, the 40 `/codex` refs sampled out of 448, the `ss -tn` on `:8003`, the 1-2-line cockpit. **Replay them, never copy them forward** — this thread has already seen its recipe lie once.
8. **Any SHA before 2026-08-17 is dead**: history was rewritten at root `f4b83c7` ("feat: initial public release — brain-v42 0.2.0"). `3ec7aaf1`, `7508546e`, `90d50f4`, `c5fd9e1` no longer resolve. **Cite file:line, not hashes** — at least 3 ticket bodies are affected.
9. **`docs/design/` is UNTRACKED** (`git status: ?? docs/design/`), as is `docs/upstream/2026-08-19-b8-session-join-rejeu.md`. Six dispositions rely on these documents. Only the **decisions in the database** (`c5160259`, `9d22bc6a`, `567f6298`, `1bc19052`, `ee271fbf`, `fc8651ad`, `4890a475`, `bc5610e6`) are durable. Publishing the directory is an operator decision — **it is not a prerequisite for any of the 4 closures**.
10. **No MCP tool edits a ticket body** (`cabb7503`, A13): **all** the reframes in section 5 will go through thread messages, so **12 stale bodies will remain readable and lying** at the head of the ticket. This is the failure mode this triage has now documented three times (`52d6b319` head-037, `eb067b57` 24/25, `c60d023d` "046").
11. **Lane constraints to strictly observe**: never two Alembic heads in flight (focus rev 214) → `2547b4a2` batch B, `b93e32be` (ii)/(iii), `b68b9692`, `259cfbe5`, `76e11c9f`, `c60d023d` are **all forbidden** while 046 is in flight. And **nothing heavy two hours before a ten-project night** (`1c40c36a`, `d45689a3`).
12. **Two active worker worktrees** (`.claude/worktrees/`) ignored as requested; all code reads are against the main tree at `14c0385`, prod measured at `045`.
