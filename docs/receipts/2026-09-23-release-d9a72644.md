# Immutable release d9a72644 — production receipt

**Status: FINAL — 0.6.1 is live and production is at schema 056.**
Window `20260923T003552Z`. Evidence directory
`~/.local/state/brain-v42-delivery/d9a72644a0e2ec8eb77ab8c5e08c6e4241c3efef/20260923T003552Z`.
Executed by `release-script-056.sh` (modes `build` → `prove` → `cutover`), retained in the
evidence directory and in `~/.local/state/brain-v42-delivery/handoff-2026-09-23/`
(predecessor `c59389e3`, `MEMBER_VERSION=0.3.0`, `PROBE_PR=175`).

This is the **schema-change path**, 055 → 056 (project archival). The first attempt at this
window, earlier the same night from `ff6e1a07`, stopped at its recovery proof (ticket
`1ec33903`): the v13 `pg_restore` twin expected `knowledge_claim_current` in the chain form,
so no real restore of a database at 055 or later could pass it. #185 fixed the twin; this
release is its merge commit.

## What it makes live

| PR | What it ships |
| --- | --- |
| **#171** | scan paths must be absolute at write time (existing rows are not repaired) |
| **#175** | migration 056 — `project_contexts.archived_at` / `archived_reason` (inert on the views) |
| **#176** | plan index inventory, repair and refresh |
| **#179** | B3 `brain_claim_verify` and the claim verdict ledger |
| **#181** | embedding token usage preserved by intent in the metrics sidecar |
| **#182** | version bump to 0.6.1 |
| **#183**, **#184** | delivery CI on GitHub-hosted runners, Dockerfile workspace member fix |
| **#185** | v13 `pg_restore` twin attests `knowledge_claim_current` in its restored form |

Verified in the installed code, not in the diff: `alembic/versions/056_project_archival.py`,
`archived_at` in `repositories/pg_project_context.py`, `mcp/tools/claim_tools.py`,
`repositories/pg_claim_verdicts.py`, `maintenance/plan_index_repair.py` and
`services/embedding_usage.py` are all present under
`releases/d9a72644…/venv/lib/python3.12/site-packages/brain_v42/`.

## Release record

The release is `d9a72644a0e2ec8eb77ab8c5e08c6e4241c3efef`, the merge commit of #185 into
`main`, built from a clean worktree detached at that SHA — not from the canonical checkout,
which carries an operator's uncommitted file. Predecessor `c59389e3cdf08d595e3c14f967f9e52291a726c3`,
measured on the eight `ExecStart` at build time.

| Input | Measured value |
| --- | --- |
| Version in `pyproject.toml` and `uv.lock` | `0.6.1`; `headless-agents` `0.3.0` (unchanged) |
| Build interpreter | `/opt/red-python/cpython-3.12.12/bin/python3.12` |
| Shipped Alembic head (smoke) | `056` |
| Production Alembic head | `055` before the window, `056` after |
| Pinned assets | migration 056, v12, v13, v13 twin and v11 ACL digests all matched the script's pins |
| Drop-ins | seven of eight byte-identical to the live ones with the SHA masked; `brain-mcp-http` differs by exactly the declared `package_version` (`0.6.0` → `0.6.1`) |

Artifact digests (SHA-256):

| Artifact | Digest |
| --- | --- |
| `brain_v42-0.6.1-py3-none-any.whl` | `14b16d75794d2387b312ca0f82ebee42de87f4736cf15606e45c8acae59096ca` |
| `headless_agents-0.3.0-py3-none-any.whl` | `12ac708ca9860f33498992d541f8cbf2ac19e88a2f31cfeefbea408391309618` (unchanged) |
| `brain-v42.tar.gz` (source archive) | `d7e8f1d5ec875f18d4895753a6831d15916e2d5365fdc11123584d3b6947950e` |
| `delivery-release.json` (manifest) | `e430b0dc49cb1451794ccab93346ba84b5c3eb23eae1653bea65c11db00e98ad` |

The manifest binds 354 package payload files, 74 source payload files and the workspace
member's 15.

## Recovery proof (before touching production)

`prove`, 00:36:02 → 00:36:20 UTC, read-only for production:

- fresh owner-only `pg_dump -Fc` of production at 055, SHA-256
  `2c36eaf1a320b88f003cac19004a0351f1effd7d75148f2a514d0bad806bb934`
  (globals `103df28d27bd7d89129fcbf77aeea822da6743ee2898aa472cc8fd60ec808e36`);
- **real restore** into a disposable clone (pinned PG16 image, tmpfs, no port, no volume);
- clone **as restored** (055) proved by **v12**: 29/30, the single failure being the accounted
  `view_definition_mismatches: 1` on `knowledge_claim_current` (v12 is kept byte-identical
  for lineage); ACL v11: zero failure;
- clone **migrated 055 → 056** with the installed wheel, proved by the **v13 twin: 30/30**;
  ACL v11: pass;
- the owned disposable container removed.

## Cutover

00:36:25 → 00:36:36 UTC (02:36 CEST — outside 06:00–09:00 and 13:00; `brain-v42-dream` was
`inactive`, its timer due at 06:00 CEST). Four active triggers frozen, the eight writers stopped
and each confirmed at `MainPID=0`, private targets attested, **Alembic 055 → 056 applied at
00:36:26Z** and post-checked (two columns, the index, the constraint, zero archived row), the
reviewed drop-ins published, `brain-mcp-http` and `brain-metrics` started dormant, dormant
preflight `status=ok` / `source_sha=d9a72644…` / `schema_revision=056`, triggers restored with
the watchdog last, observer restarted. `unit-state.before` and `unit-state.after` are
byte-identical (SHA-256 `8f3210aff5a830c2…`). All eight writers measured on the new SHA.

`brain-v42-model-liveness.service` was `failed` before the window and is `failed` after:
pre-existing, neither caused nor repaired here.

## Acceptance

- `/health`: `version 0.6.1`, `alembic_head 056`.
- **DR contract v13 against production: 30/30**
  (`v13-production-result.json`, SHA-256
  `9b56333ca3dc359224878280bb76c8ee74f3bb09eab6bab0248296744957285c`); ACL v11 against
  production: 1/1.
- `facts.registry_frozen` lists the six facts; `knowledge_fact_definitions` still holds 6 rows.
- Zero `error` or `critical` line in the new MCP process journal.

The **twin** (`brain-v42-v13-pgrestore.sql`) was also run against production by mistake and
scored 28/30 (`view_definition_mismatches: 1`, `table_index_mismatches: 1`). That is expected
and not a finding: the twin carries restored-form fingerprints by construction and attests a
restored database, while production renders the chain form the base contract attests. The
result is kept as `v13-pgrestore-twin-against-production.json`; it proves nothing either way.

## Rollback

`SOURCE_TREE=<worktree> PROBE_PR=175 release-script-056.sh rollback d9a72644a0e2ec8eb77ab8c5e08c6e4241c3efef 20260923T003552Z`
republishes the backed-up `c59389e3` drop-ins and restarts the MCP and metrics. **Code-only:
056 stays applied**, and `c59389e3`'s own preflight refuses 056 — there is no roll-forward
back to it. A real schema rollback means restoring the pre056 dump above.
