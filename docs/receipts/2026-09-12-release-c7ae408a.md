# Immutable release c7ae408a — production receipt

**Status: FINAL — the release is live; its first scheduled Dream night is due 2026-09-13 06:00 CEST, and an on-demand DRY canary already ran on it.**
Window `20260912T133009Z`. Evidence directory
`~/.local/state/brain-v42-delivery/c7ae408a8ef6492c6dd5ccd005bf40e04972b530/20260912T133009Z`.
Procedure: `docs/runbooks/2026-09-07-observable-delivery-workflows.md`, subsequent-release path
(no schema change; private-configuration, recovery-proof, migration and activation blocks not
run), executed by the same three-mode script as release a20265ac, retained as `release-script.sh`
in the evidence directory.

This release carries no schema change. It makes pull request #139 reachable by the eight systemd
writers: lot 1 of the agent runtime extraction, delivered under Brain ticket `c31bad72` with
delivery contract revision 1 — the first change to reach production through the observable
delivery workflow since the canary day of 2026-09-08.

## Release record

The release is `c7ae408a8ef6492c6dd5ccd005bf40e04972b530`, the merge commit of pull request #139
into `main`. Its predecessor was `a20265ac2c3247e1b2476cdabdafc8ed0ef06726`.

The merged `main` revision was verified on the host before the build: 10137 unit tests passed
(107 skipped), `ruff check`, `ruff format --check` and `mypy src/` clean, 396 dream and delivery
integration tests passed against `brain_test` at head 053 (one failure on the first run was not
reproducible on the second). `brain_test` had been left at 052 since migration 053 landed on
2026-09-08 and was upgraded before these runs; production was at 053 throughout.

| Input | Measured value |
| --- | --- |
| Version in `pyproject.toml` and `uv.lock` | `0.5.0` |
| Build interpreter | `/opt/red-python/cpython-3.12.12/bin/python3.12`, 3.12.12, regular file; `venv/pyvenv.cfg` hashed in the manifest |
| Working tree | clean at `SOURCE_SHA` |
| Repository Alembic head | `053` — unchanged |
| Manifest payload | 307 wheel files mapped to the source archive (297 in a20265ac; the ten new `brain_v42/agents/` modules), 69 retained source files |

Artifact digests (SHA-256):

| Artifact | Digest |
| --- | --- |
| `brain_v42-0.5.0-py3-none-any.whl` | `58ab9ab785a3e7ced2d8f22998f9505275cf704591b23b710fea2546a2ef776f` |
| `brain-v42.tar.gz` (source archive) | `a2c918d7e29a69c3178d15124dfc95fd5b39a873bba93e3c2b0180fb7ebebfbf` |
| `delivery-release.json` (manifest) | `5548560f9ad1aa0293aa610d1110c19e5a86d11fde430e80b742057dfabb473a` |

The smoke, run outside the checkout, asserted that `brain_v42` resolves inside the release venv,
that the installed version is `0.5.0`, that `shipped_alembic_head()` returns `053`, and that the
observer, `check_delivery_deployment.py` and `verify_delivery_canary.py` answer `--help`.

## Drop-in review

The eight rendered `90-immutable-release.conf` drop-ins and the rendered observer unit were
compared against the live ones with the release SHA masked on both sides: all byte-identical
under that mask. `PATH_TAIL`, the reaper's `--max-age-hours 48` and `WorkingDirectory=/home/hawixs`
were read from the live configuration. `91-delivery-mode.conf` was rewritten with identical
content. A pre-window guard confirmed all eight writers on the predecessor SHA. The preflight
configuration names pull request #139 as the dormant probe.

## Cutover

The window ran from 13:38:38 to 13:38:47 UTC. Four triggers were active and were recorded before
being stopped: `brain-mcp-http-watchdog.timer`, `brain-mcp-reaper.timer`, `brain-v42-dream.timer`,
`brain-v42-model-liveness.timer` (`triggers.active.before`, SHA-256
`6f66cf7839efead91d52bfe0d2ec77aaf673570b81309552eea750e3885276f3`). Three writers were active and
were stopped: `brain-mcp-http.service`, `brain-metrics.service`,
`brain-v42-delivery-observer.service`. Every writer was asserted at `MainPID=0` before
publication.

MCP health in dormant mode (SHA-256 `b1a13eb785429983784938a5694ba775a9d3a33f7ba7fa8310df760e70de0928`):

```json
{"status":"ok","version":"0.5.0","alembic_head":"053","pool":{"size":20,"checked_out":4}}
```

The dormant preflight returned, verbatim (SHA-256 `d9797af7125386570f2be6bce77711d90d0819c71625ddcb8538c137a6bb832b`):

```json
{"schema_revision":"053","source_sha":"c7ae408a8ef6492c6dd5ccd005bf40e04972b530","status":"ok","timestamp":"1789220325"}
```

Triggers were restored with the watchdog last, and the observer was started again because it was
active before the window. `unit-state.before` (SHA-256
`996a4da4abb1f5b2b0021c87795e40049fd35b39883e87326ee0f791bc1493c6`) and `unit-state.after`
(`da3142b59fc1fb72df3eddb515e4b31a5569f6e65258dd4d5581d966158e5bb0`) differ on one line:
`brain-mcp-reaper.service` read `activating` at the pre-window inventory (13:30 UTC, its timer had
just fired the oneshot) and `inactive` after the window. The enablement and activity of the other
seven writers and all five triggers are identical. All eight writers were confirmed on the new SHA.
No Dream drop-in was changed by this release.

## On-demand DRY canary (acceptance criterion 7 of contract c31bad72)

A temporary drop-in (`BRAIN_DREAM_PROJECT_POOL=brain-v42`, `DRY_RUN=true`, extract, sweep and
reorg forced dry) was installed, the Dream unit started, the running process's environment
verified to carry those five values, and the drop-in removed with a `daemon-reload` while the
run was still in its first phase, so the unit's environment for the 06:00 night (ten-project
pool, WET) was restored before the canary finished. The run lasted from 15:39:19 to 15:57:28 CEST.

| Phase | Provider / model | Outcome |
| --- | --- | --- |
| scan, clean, connect | codex, `gpt-5.6-luna` / high | done |
| synth, promote, reorg | codex, `gpt-6-astra` / max | done |
| extract | NVIDIA `nemotron-3-super-120b-a12b`, dry | done |
| sweep | none, dry | done |

`Dream finished: 8/8 phases OK`; `dream_runs` coverage `expected 8 written 8 skipped 0 declared 0
writefail 0 silent 0 mismatch 0`; `REORG VALIDATE: OK — mode=dry-run updated=0 archived=0`;
`CONNECT VALIDATE: OK`. The reconciliation line reports `pairs_written=62 gap=-54` with a warning:
it counts every `dream_runs` pair of the same `run_date`, including the 62 written by the 06:00
night, against the 8 phases of this run. That is an artifact of a second run on the same date,
not a lost insert (`mismatch 0`). The models and options in the log match the unit's live
configuration; the 06:00 night of the same day still ran on `gpt-5.6-terra` and `gpt-5.6-sol`,
because the model change of PR #135 and its drop-in were applied after it. Command equivalence
with the pre-extraction runners is what the golden oracle of PR #139 proves.

## Delivery workflow trace

Contract revision 1 set at 12:30:23 UTC; the observer confirmed the five pinned repository
documents within a minute and re-confirmed them every minute while the workflow stayed active.
PR #139 bound at 13:2x UTC with `expected_workflow_version` 50; observed with head
`76f3bea3` on base `81a6ffb4`; nine protected checks green; merged at 13:29 UTC; one
`provider_invalid_response` confirmation at 13:29:45 UTC during the merge transition, the same
signature as the nine errors of the 2026-09-08 canary; integration recorded at 13:30:48 UTC with
`integration_sha c7ae408a` and the integration receipt issued; explicit acceptance at
13:58:42 UTC after the canary, fulfilled receipt `77dc84f2`, delivery digest
`a38ae3b289558869c0c635edc6a8c2261abfc99e4263aa271f2fb7d526625dbb`.

## Rollback

`a20265ac2c3247e1b2476cdabdafc8ed0ef06726` remains retained with its dormant preflight file and is
the forward-rollback target; it was built on `/opt/red-python`.
