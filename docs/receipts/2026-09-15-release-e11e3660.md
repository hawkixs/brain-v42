# Immutable releases 60f4e807 and e11e3660 — production receipt

**Status: FINAL — release `e11e3660` is live on the eight writers; the on-demand DRY canary of
the opencode rail ran on `60f4e807` and its two findings are what `e11e3660` fixes.**

Two releases in one afternoon, same operator (Hawixs), same session, same scripted procedure as
`docs/receipts/2026-09-14-release-84138170.md` (handoff `~/.local/state/brain-v42-delivery/
handoff-2026-09-15/`, `MEMBER_VERSION=0.2.0`, `VERSION=0.6.0` unchanged). Feature: the fourth
headless-agent provider, `opencode` (opencode.ai CLI 1.18.30 under the OpenCode Go subscription),
added to `packages/headless-agents` (0.1.0 → 0.2.0) and wired as the second link of the Dream
chain, `codex,opencode,agy,claude`. Brain decision `38d394da`, learning `bf7f26e2`.

## Release record

| | `60f4e807` | `e11e3660` |
|---|---|---|
| Source | merge of PR #145 (`feat/opencode-provider`, 8 commits, CI 9/9) | merge of PR #146 (`fix/opencode-canary-followups`, CI 9/9) |
| Predecessor | `84138170` | `60f4e807` |
| Window (UTC) | `20260915T140931Z`, cutover 14:09:48 → 14:09:57 | `20260915T143457Z`, cutover 14:35:03 → 14:35:12 |
| Wheels | `brain_v42-0.6.0` `c0dc654f…`, `headless_agents-0.2.0` `4de5e60b…` | `brain_v42-0.6.0` `c0dc654f…`, `headless_agents-0.2.0` `76847325…` |
| Dormant health | `{"status":"ok","version":"0.6.0","alembic_head":"053"}` (`e381a39b…`, unchanged) | same bytes |
| Dormant preflight | `{"schema_revision":"053","source_sha":"60f4e807…","status":"ok"}` (`e1032735…`) | `{…"source_sha":"e11e3660…"…}` (`35a03260…`) |
| Drop-ins | 8 rendered, identical to live under SHA mask | idem |
| `unit-state` before = after | `3ccd1421…` | `8f3210af…` (the Dream unit had been `reset-failed` between the two) |
| Triggers frozen and restored | 4 (`6f66cf78…`, same list as every release since 09-13) | idem |

The main wheel's digest is identical across the two releases: PR #146 touched only the member
(`headless_agents.providers.opencode.extract_report`) and `scripts/dream/reorg_events.py`, which
ships in the source payload, not the wheel.

## Drop-in change — the one operator gesture of this delivery

`~/.config/systemd/user/brain-v42-dream.service.d/killswitches.conf` (backup
`killswitches.conf.bak-20260915-1610`):

```
Environment=BRAIN_DREAM_AGENT_PROVIDERS=codex,opencode,agy,claude
Environment=BRAIN_DREAM_OPENCODE_BIN=%h/.opencode/bin/opencode
```

Measured after `daemon-reload` on the live unit's `Environment=`: the four-link chain, the resolved
binary `/home/hawixs/.opencode/bin/opencode`, the ten-project pool, every WET killswitch as before,
and **no `zz-` drop-in** (`DropInPaths` counted 0). The first night of the four-link chain is
2026-09-16 06:00, on `e11e3660`; expected, while the Codex quota stays exhausted until 09-19: every
agent phase started on codex, `exit=3` in 3-4 s, taken by opencode.

## On-demand DRY canary (release `60f4e807`, 16:10 → 16:18 CEST)

Temporary drop-in `zz-canary-opencode.conf` (`594d9f47…`): pool reduced to `brain-v42`, every
mutating phase forced DRY, and the chain forced to `opencode,agy,claude` — codex removed so the
link under test sets off first, as on the forced-fallback reference canary of 2026-09-13. The
drop-in was removed with a `daemon-reload` at 16:19, before any timer could see it.

Result: `6/8 phases OK, 2 failed (brain-v42/connect */extract)`. Every one of the six agent phases
ran on opencode (`START … provider=opencode, model=opencode-go/glm-5.3-flash`), the deep tier at
`--variant high`; no fallback fired. `dream_runs` carries six `opencode-go/glm-5.3-flash` rows
(`canary-dream_runs.txt`, `bf1d822f…`), the first rows of this rail and the first with a measured
`cost_usd`:

| phase | status | fresh in | cached | out | cost $ | calls | tool calls | s |
|---|---|---|---|---|---|---|---|---|
| scan | done | 9 333 | 3 840 | 810 | 0.0010 | 2 | 4 | 31 |
| clean | done | 32 746 | 61 184 | 1 997 | 0.0039 | 5 | 6 | 43 |
| connect | partial | 4 767 | 4 352 | 123 | 0.0005 | 2 | 1 | 31 |
| synth | done | 12 965 | 39 168 | 2 552 | 0.0022 | 6 | 10 | 45 |
| promote | done | 25 819 | 69 632 | 4 432 | 0.0041 | 4 | 5 | 124 |
| reorg | done | 293 534 | 111 872 | 6 685 | 0.0254 | 5 | 24 | 145 |

Total 0.037 $ for one project, DRY. Extrapolated to the ten-project pool, well under the weekly
30 $ cap of a 60 $-cap Go model even if codex stays down all week; the cap is visible only in the
opencode.ai console (Weekly tab), never through the CLI.

What the canary proves: the scoped `(project, phase)` bearer reached the server (the scan phase
saw its four tools and nothing else); the actor header travels end to end — `access_log` carries
`dream-opencode-reorg` on 7 `get_by_id` rows in the window; **no write** — zero rows with
`content_updated_at` or `freshness_status_updated_at` in the window on learnings and decisions,
the only `dream_promotions` row being the `dry_run rehearsal` record PROMOTE writes on purpose,
and the one learning created in the window belonging to `red-games`, a project outside the pool,
from another session.

The two failures. `*/extract` is the pre-existing embedding backlog of project `red` (four
learnings without a vector; the dedup gate is fail-closed), unrelated to opencode and still open.
`brain-v42/connect` did its work (STEP_A/STEP_B lines correct, one tool call) but glm-5.3-flash
wrapped the two lines in ``` fences and the strict validator counted four. One warning:
`REORG SYMMETRY WARN … no recognisable codex or agy tool call — symmetry UNVERIFIED` on a stream of
24 opencode tool calls, the reader knowing two dialects. Both are what PR #146 closes: the
runtime now drops a fence around the WHOLE report (never one inside prose), and
`reorg_events` reads the third dialect (`tool_use` part named `brain-v42_<tool>`, arguments under
`state.input`, counted once per part id in the `completed` state). Both fixes were replayed on the
canary's own streams before the PR: the two STEP lines exactly; `recognised=True`, 24 events.

Log retained as `canary-dry-opencode-2026-09-15.log` (`b2d9351c…`), phase lines as
`canary-2026-09-15.phase-lines` (`395c44b2…`), under the `60f4e807` evidence directory.

## Independent review

Opus review in two passes before PR #145 (16 findings, 15 closed, one documented convention:
unset `BRAIN_DREAM_OPENCODE_*_MODEL` variables are `dream.sh`'s to default, as for codex). The
review's most severe finding — the Dream provider's `child_environment()` handing out an
environment without the inline config, which a protocol consumer could have paired with
`build_command()` to launch opencode against the real HOME — is closed; no consumer did so.

## Rollback

`84138170` remains the target below `60f4e807` and `e11e3660`; the handoff script's `rollback`
mode republishes the backed-up drop-ins of `60f4e807` (predecessor of `e11e3660`) and restarts.
Reverting the feature itself is the drop-in line `BRAIN_DREAM_AGENT_PROVIDERS=codex,agy,claude`
(the backup of 16:10), a `daemon-reload`, and nothing else: the release carries the rail, the
drop-in arms it.
