# Immutable release e3777f59 — production receipt

**Status: FINAL — the release is live; the facts canary of spec §7.2 passed 16 seconds after
the window, the briefing line is rendered at the next explicit session lifecycle command.**
Window `20260920T101500Z`. Evidence directory
`~/.local/state/brain-v42-delivery/e3777f5912886df19e5a5ddccefe6b67536db364/20260920T101500Z`.
Procedure: `docs/runbooks/2026-09-07-observable-delivery-workflows.md`, subsequent-release path
(no schema change), executed by the 2026-09-19 script with three anchors patched (predecessor
`b4f7194d`, `MEMBER_VERSION=0.3.0`, `PROBE_PR=159`), retained as `release-script.sh` in the
evidence directory and as `~/.local/state/brain-v42-delivery/handoff-2026-09-20/`.

This release carries no schema change. It makes two merged pull requests reachable by the
eight systemd writers:

- **#158** (`feat/facts-core`, lot A slice 1 of ADR #27): the measured-facts registry, its two
  MCP tools `brain_fact_list` / `brain_fact_get`, the first fact `graph_projection_lag` bound to
  the verified production identity, and its line in the session briefing.
- **#159** (`fix/dream-dead-link-fallthrough`, Brain ticket `f4277a90`): a Dream provider link
  that expires before any Brain call now falls through to the next link (headless-agents 0.3.0,
  exit code 4) and is retired for the rest of the night (`LINK DOWN`). On 2026-09-19 a quota-dead
  opencode link had cost 37 phases and seven projects.

The same window also regenerated the delivery observer registry for `hawkixs/red-probe`
(runbook `434dc417`, requested by red-rail): 25 → 26 keys, 48 → 50 entries, observer restarted at
10:00:30 UTC and `brain-mcp-http` at 10:00:53 UTC, both verified in the live process environment;
`brain_delivery_get` for ticket `1321ba58` answered `contract_not_found` instead of
`unknown_repository`. The registry lives in the two env files, not in the release, and survived
the cutover.

## Release record

The release is `e3777f5912886df19e5a5ddccefe6b67536db364`, the merge commit of pull request #159
into `main`. Its predecessor was `b4f7194d84c153dad11ee249ec9eca8951056651` (2026-09-19),
measured on the `ExecStart` of `brain-mcp-http` and of `brain-v42-dream` before the build; all
eight writers were on it at build time (pre-window guard of the script).

The merged `main` revision was verified before each pull request: for #159, 10 673 unit tests
passed (107 skipped), `ruff check`, `ruff format --check` and `mypy src/ packages/headless-agents/src/`
clean, the nine CI jobs green, and an independent review (red-reviewer, opus) with the tests
re-read — verdict "mergeable after should-fixes", the four should-fixes closed in the third
commit of the branch. Production was at 054 throughout.

| Input | Measured value |
| --- | --- |
| Version in `pyproject.toml` and `uv.lock` | `0.6.0`; `headless-agents` `0.3.0` (bumped by #159) |
| Build interpreter | `/opt/red-python/cpython-3.12.12/bin/python3.12` |
| Working tree | clean at `SOURCE_SHA` |
| Repository Alembic head | `054` — unchanged |
| Drop-ins | eight rendered `90-immutable-release.conf` and the observer unit byte-identical to the live ones with the SHA masked |
| `zz-` drop-ins at build and at cutover | none |
| New drop-in | `brain-mcp-http.service.d/92-facts-identity.conf` (below), installed before the cutover's `daemon-reload` |

Artifact digests (SHA-256):

| Artifact | Digest |
| --- | --- |
| `brain_v42-0.6.0-py3-none-any.whl` | `1f9acb316a4f9f51845f81291355bf6c66221ac006a4bdfec214f5fc19626a46` |
| `headless_agents-0.3.0-py3-none-any.whl` | `3500ee36b4ff2a8e44f79cf9a593c9d3951ae0b48c7294ad60ad224857a7709b` |
| `brain-v42.tar.gz` (source archive) | `84bd7e733d330b4032d6ed778b5570893e80234542f2605b610159cc5dda0775` |
| `delivery-release.json` (manifest) | `e4885bfcb68d395aa7426a9f949ecdaa13170403bf53b4f6aa8293c0688dcc4f` |

The smoke, run outside the checkout, asserted that `brain_v42` and `headless_agents` resolve
inside the release venv, that the installed versions are `0.6.0` and `0.3.0`, and that
`shipped_alembic_head()` returns `054`. The contract tag `headless-agents-v0.3.0` was posted on
the merge commit.

## The facts identity declaration

Spec §5.3 rule 4: the identity a production probe must find is declared by the operator,
never derived from the DSN. Measured on 2026-09-20 through the application's own DSN
(`localhost:5433/brain`), identical to the 2026-09-20 morning measurement:

```
BRAIN_FACTS_PRODUCTION_IDENTITY={"system_identifier":"7612696091383607335","database":"brain","server_addr":"172.31.0.4","server_port":5432}
BRAIN_FACTS_HOST_IDENTITY={"hostname":"hawixspcicube"}
```

Both are non-secret and live in `~/.config/systemd/user/brain-mcp-http.service.d/92-facts-identity.conf`
(single-quoted `Environment=` assignments; the rendered `Environment` property of the unit is
799 bytes, and `scripts/check_delivery_deployment.py`'s parser accepts it — proved before the
cutover). The host identity is consumed only by lot A slice 2, in progress; the running release
ignores it. `server_addr` is the container's own address: a recreated container can change it,
and every production fact then answers `unreadable (target_mismatch)` until the operator
re-declares it. The role `brain` is a superuser at this date (`rolsuper=t`), which is why
`pg_control_system()` is executable; narrowing that grant is an open operator decision.

## Build and cutover

Build: 10:02:43 → 10:02:49 UTC, production untouched.

Cutover: 10:03:11 → 10:03:20 UTC (12:03 CEST, outside the 06:00–09:00 window) — quiesce of the
four active triggers and the eight writers, publication of the reviewed drop-ins, dormant start of
`brain-mcp-http` and `brain-metrics`, dormant preflight (`status=ok`, `source_sha=e3777f59…`,
`schema_revision=054`), triggers restored with the watchdog last, observer restarted because it
was active before the window. `unit-state.before` and `unit-state.after` are byte-identical
(SHA-256 `da3142b59fc1fb72…` on both sides). All eight writers measured on the new SHA;
`/health` of the dormant MCP reported `version 0.6.0`, `alembic_head 054`.

`brain-v42-dream.timer` was restored for 06:00 CEST on 2026-09-21: the first night that runs
with the dead-link fallthrough.

## Acceptance: the facts canary (spec §7.2)

The journal of `brain-mcp-http` (PID 878614) at start: `facts.registry_frozen
facts=['graph_projection_lag']`, no identity warning. The live process environment carries the
two declarations.

| Step | Result |
| --- | --- |
| `brain_fact_get("graph_projection_lag", max_age_seconds=0)` at 10:03:36 UTC | `status=measured`, `source_kind=probe`, `observation_id=fd1e1000-1e42-495e-b8e1-523edf7a7ecc`, `duration_ms=4`, `source` equal to the declaration, value `pending=0, ready=0, claimed=0, exhausted=0, lag_seconds=0, generation=96, armed=true, lease_active=true, recovery_active=false, healthy=true`, digest `e750ccb0…` |
| second call within 15 s, `max_age_seconds` unset | `source_kind=cache`, the **same** `observation_id` and digest |
| `brain_fact_list()` | one descriptor, `briefing=true`, `deadline_seconds=5`, `last.status=measured` |
| Briefing line `- Projection graphe : aucun retard observé dans l'outbox — 0 en attente, génération 96 armée, bail tenu` | rendered by the next explicit `brain session start`/`resume` (the lifecycle stays a user command) |

## Rollback

Same script, mode `rollback`, same window id: republishes the backed-up `b4f7194d` drop-ins and
restarts `brain-mcp-http` and `brain-metrics`. The `92-facts-identity.conf` drop-in is inert on
the predecessor (the setting is unknown to it) and may stay.
