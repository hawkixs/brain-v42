# Immutable release a20265ac — production receipt

**Status: FINAL — the release is live; its first Dream night is due 2026-09-13 06:00 CEST.**
Window `20260912T105045Z`. Evidence directory
`~/.local/state/brain-v42-delivery/a20265ac2c3247e1b2476cdabdafc8ed0ef06726/20260912T105045Z`.
Procedure: `docs/runbooks/2026-09-07-observable-delivery-workflows.md`, subsequent-release
path (no schema change; the private-configuration, recovery-proof, migration and activation
blocks were not run, because the observer file already carries the GitHub App triplet and the
registry, and the MCP delivery file already carries `BRAIN_DELIVERY_ENABLED=true`). The two-phase
script that executed the build and the window is retained as `release-script.sh` in the evidence
directory.

This release carries no schema change. It makes three merged pull requests reachable by the
eight systemd writers:

- #135 — the nightly models: phase 1 (SCAN, CLEAN, CONNECT) on `gpt-5.6-luna` at `high`,
  phase 2 (SYNTH, PROMOTE, REORG) on `gpt-6-astra` at `max`, the agy fast fallback on
  `gemini-3.8-flash-high`; the Codex runner accepts the `max` and `ultra` reasoning efforts.
- #136 — `extract` gains a third link on a transport independent of NVIDIA: the agy CLI
  (Gemini) in tool-less print mode, reached by a run-level switch when both NVIDIA models are
  gone and by a ticket-level rescue after a transport failure.
- #137 — the observer cutover runbook and receipts, and the receipt of release 96676164.

## Release record

The release is `a20265ac2c3247e1b2476cdabdafc8ed0ef06726`, the merge commit of pull request
#137 into `main`. Its predecessor was `9667616404dc932b697c378502397d27cfb8a84e`.

The nine required checks passed on each of the three pull requests, but CI tests one pull
request at a time; the merged `main` revision was therefore verified on the host before the
build: 10108 unit tests passed (107 skipped), `ruff check`, `ruff format --check` and
`mypy src/` clean.

| Input | Measured value |
| --- | --- |
| Version in `pyproject.toml` and `uv.lock` | `0.5.0` |
| Build interpreter | `/opt/red-python/cpython-3.12.12/bin/python3.12`, 3.12.12, regular file; `venv/pyvenv.cfg` hashed in the manifest |
| Working tree | clean at `SOURCE_SHA` |
| Repository Alembic head | `053` — unchanged |
| Manifest payload | 297 wheel files mapped to the source archive (296 in 96676164; the new `brain_v42/scripts/agy_completion.py`), 69 retained source files |

Artifact digests (SHA-256):

| Artifact | Digest |
| --- | --- |
| `brain_v42-0.5.0-py3-none-any.whl` | `b8c70efaafa5844fd8d2e54d2da3fee9ba8fb9f567dde4bdd7e310c91f10ee90` |
| `brain-v42.tar.gz` (source archive) | `933ba09f3bd5252186a63d59b6a5cd62a4c43712af75ada260b305ebefeebea0` |
| `delivery-release.json` (manifest) | `d40a6f6bd891592e1a2f679eaf9a9cc009f394219a7cd5a2c338936beca555a9` |

The smoke, run outside the checkout, asserted that `brain_v42` resolves inside the release
venv, that the installed version is `0.5.0`, that `shipped_alembic_head()` returns `053`, and
that the observer, `check_delivery_deployment.py` and `verify_delivery_canary.py` answer
`--help`.

## Drop-in review

The observer unit was rendered by `install-delivery-observer.sh` (`--check-only`, then
`--render-dir`). The eight rendered `90-immutable-release.conf` drop-ins and the rendered
observer unit were compared against the live ones with the release SHA masked on both sides:
all byte-identical under that mask, so the only change published to systemd was the release
path. `PATH_TAIL`, the reaper's `--max-age-hours 48` and `WorkingDirectory=/home/hawixs` were
read from the live configuration. `91-delivery-mode.conf` was rewritten with identical content.

A pre-window guard confirmed all eight writers on the predecessor SHA in their effective
`ExecStart`. The preflight configuration
`delivery-preflight-a20265ac2c3247e1b2476cdabdafc8ed0ef06726.json` names pull request #136 as the
dormant probe.

## Cutover

The window ran from 10:54:10 to 10:54:19 UTC. Four triggers were active and were recorded before
being stopped: `brain-mcp-http-watchdog.timer`, `brain-mcp-reaper.timer`, `brain-v42-dream.timer`,
`brain-v42-model-liveness.timer` (`triggers.active.before`, SHA-256
`6f66cf7839efead91d52bfe0d2ec77aaf673570b81309552eea750e3885276f3`). Three writers were active and
were stopped: `brain-mcp-http.service`, `brain-metrics.service`,
`brain-v42-delivery-observer.service`. Every writer was asserted at `MainPID=0` before
publication. The ten replaced files were backed up under `systemd-before/`.

MCP health in dormant mode (SHA-256 `6bdc0bfb5bba4bcd4980d396a94b75dc4fd5da822455a42c55e731bdf4e43690`):

```json
{"status":"ok","version":"0.5.0","alembic_head":"053","pool":{"size":20,"checked_out":0}}
```

The dormant preflight returned, verbatim (SHA-256 `9aab4c936c41b69a6d1f13734293eb22604b3452dbcf857aec0c484cc1f32cdf`):

```json
{"schema_revision":"053","source_sha":"a20265ac2c3247e1b2476cdabdafc8ed0ef06726","status":"ok","timestamp":"1789210457"}
```

Triggers were restored with the watchdog last, and the observer was started again because it
was active before the window. `unit-state.before` and `unit-state.after` share the SHA-256
`da3142b59fc1fb72df3eddb515e4b31a5569f6e65258dd4d5581d966158e5bb0`: the enablement and activity
of all eight writers and all five triggers are identical across the window. All eight writers
were confirmed on the new SHA.

## Post-window: Dream model drop-ins

With the runner of #135 live, `~/.config/systemd/user/brain-v42-dream.service.d/codex-models.conf`
moved `BRAIN_DREAM_CODEX_DEEP_REASONING` from `xhigh` to `max` (the previous file is retained as
`codex-models.conf.before` in the evidence directory), and `agy-models.conf` was removed because
the release carries `gemini-3.8-flash-high` as the code default (retained as
`agy-models.conf.removed`). After `daemon-reload` the unit's effective environment carries
`gpt-5.6-luna` / `high` and `gpt-6-astra` / `max`, which are also the defaults of the retained
`dream.sh`. Dream logs now live under
`releases/a20265ac2c3247e1b2476cdabdafc8ed0ef06726/brain-v42/logs/dream/`.

## Rollback

`9667616404dc932b697c378502397d27cfb8a84e` remains retained with its dormant preflight file and is
the forward-rollback target; it was built on `/opt/red-python`. `079dc10bd01ed7281ded77451abdfa5e93ef51c1`
is still retained and still resolves its standard library from the uv-managed interpreter tree.
