# Immutable release 96676164 — production receipt

**Status: FINAL — the release is live; its first Dream night is due 2026-09-12 06:00 CEST.**
Window `20260911T213808Z`. Evidence directory
`~/.local/state/brain-v42-delivery/9667616404dc932b697c378502397d27cfb8a84e/20260911T213808Z`.
Procedure: `docs/runbooks/2026-09-07-observable-delivery-workflows.md`, subsequent-release
path (no schema change; the private-configuration and activation blocks were not run, because
the observer file already carries the GitHub App triplet and the 44-entry registry, and the MCP
delivery file already carries `BRAIN_DELIVERY_ENABLED=true` plus the same registry).

This release carries no schema change. It makes two merged pull requests reachable by the eight
systemd writers: #133 (the roadmap phase leaves the nightly rail) and #134 (observer credential
hardening after the GitHub App cutover: cached installation token dropped on a refused data
request, PAT-plus-App files refused by the loader, App triplet loading pinned by tests).

## Release record

The release is `9667616404dc932b697c378502397d27cfb8a84e`, the merge commit of pull request
#134 into `main`. Its predecessor was `079dc10bd01ed7281ded77451abdfa5e93ef51c1`.

| Input | Measured value |
| --- | --- |
| Version in `pyproject.toml` and `uv.lock` | `0.5.0` |
| Build interpreter | `/opt/red-python/cpython-3.12.12/bin/python3.12`, regular file, `root:root 0755`, 3.12.12 — **first release built on the root-owned interpreter** (Brain decision `296a22ce`); `venv/pyvenv.cfg` has `home = /opt/red-python/cpython-3.12.12/bin` and is hashed in the manifest |
| Working tree | clean at `SOURCE_SHA` |
| Repository Alembic head | `053` — unchanged |
| Manifest payload | 296 wheel files mapped to the source archive, 69 retained source files |

Artifact digests (SHA-256):

| Artifact | Digest |
| --- | --- |
| `brain_v42-0.5.0-py3-none-any.whl` | `f9a142b95e554f7c16ad2d006f0be15a1400c92f49a78a820f6e8ccb9dbb6ca1` |
| `delivery-release.json` (manifest) | `a0763a4737d9fbc8f3b914d6a7d03925390b1a99de7c11569455880a8db52dd6` |

The smoke asserted that `brain_v42` resolves inside the release venv, that the installed
version is `0.5.0`, that `shipped_alembic_head()` returns `053`, and that the observer,
`check_delivery_deployment.py` and `verify_delivery_canary.py` answer `--help`.

## Drop-in review

The observer unit was rendered by `install-delivery-observer.sh` (`--check-only`, then
`--render-dir`; `systemd-analyze --user verify` needs the user bus environment, which the
runbook's shell inherits and a fresh shell does not). The eight rendered
`90-immutable-release.conf` drop-ins and the rendered observer unit were compared against the
live ones with the release SHA masked on both sides: all byte-identical under that mask, so the
only change published to systemd was the release path. `PATH_TAIL`, the reaper's
`--max-age-hours 48` and `WorkingDirectory=/home/hawixs` were read from the live
configuration. `91-delivery-mode.conf` was rewritten with identical content.

A pre-window guard confirmed all eight writers on the predecessor SHA in their effective
`ExecStart`.

## Cutover

Four triggers were active and were recorded before being stopped:
`brain-mcp-http-watchdog.timer`, `brain-mcp-reaper.timer`, `brain-v42-dream.timer`,
`brain-v42-model-liveness.timer` (`triggers.active.before`, SHA-256 `6f66cf7839efead91d52bfe0d2ec77aaf673570b81309552eea750e3885276f3`). Three writers
were active and were stopped: `brain-mcp-http.service`, `brain-metrics.service`,
`brain-v42-delivery-observer.service`. Every writer was asserted at `MainPID=0` before
publication.

MCP health in dormant mode (SHA-256 `6bdc0bfb5bba4bcd4980d396a94b75dc4fd5da822455a42c55e731bdf4e43690`):

```json
{"status":"ok","version":"0.5.0","alembic_head":"053","pool":{"size":20,"checked_out":0}}
```

The dormant preflight returned, verbatim (SHA-256 `d297c323704732e5bf5fa9e6699b54b0c1627b09d7fb72813f1754fcb10522ee`):

```json
{"schema_revision":"053","source_sha":"9667616404dc932b697c378502397d27cfb8a84e","status":"ok","timestamp":"1789162842"}
```

This is the first preflight whose remote ancestry check ran with the GitHub App credential
rather than a PAT: the checker goes through `GitHubAuthProvider` and the observer file in App
mode, and the new loader guard (PAT-plus-App refusal) is on the path it took.

Triggers were restored with the watchdog last, and the observer was started again because it
was active before the window. `unit-state.before` and `unit-state.after` share the SHA-256
`da3142b59fc1fb72df3eddb515e4b31a5569f6e65258dd4d5581d966158e5bb0` — the enablement and activity of all eight writers and all five triggers are identical
across the window. All eight writers were confirmed on the new SHA. The four runtime flags
carried by separate drop-ins survived unchanged (`BRAIN_DELIVERY_ENABLED`,
`BRAIN_SESSION_DERIVED_CAPTURE_ENABLED`, `BRAIN_SESSION_AUTO_OPEN_ENABLED`,
`CLIENT_ACTIVITY_REPORTING_ENABLED`, all `true`), the MCP process carries the 44-entry
repository registry, and the observer process carries the App triplet and no PAT.

## Post-window: roadmap killswitches retired

With #133 live, nothing reads `BRAIN_DREAM_ROADMAP_ENABLED` and
`BRAIN_DREAM_ROADMAP_DRY_RUN` any more, and their presence alone kept the briefing
announcing the phase. Both lines were removed from
`~/.config/systemd/user/brain-v42-dream.service.d/killswitches.conf` (backup
`killswitches.conf.bak-20260911-2341` kept beside it) and the manager reloaded; the unit's
effective environment carries no `ROADMAP` key. The retained `dream.sh` mentions
`roadmap` once, in a comment.

## Rollback

`079dc10bd01ed7281ded77451abdfa5e93ef51c1` remains retained with its dormant preflight file
and is the forward-rollback target; its venv resolves its standard library from the uv-managed
interpreter tree, which must therefore not be uninstalled while that release is a rollback
candidate.
