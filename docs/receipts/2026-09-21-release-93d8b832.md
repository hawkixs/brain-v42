# Immutable release 93d8b832 — production receipt

**Status: FINAL — the release is live; the shipped head and the production head are equal
again, and the three claim ledgers of migration 055 stopped being inert.**
Window `20260921T173000Z`. Evidence directory
`~/.local/state/brain-v42-delivery/93d8b83207a9251a454a0d0a542859107a251aab/20260921T173000Z`.
Procedure: `docs/runbooks/2026-09-07-observable-delivery-workflows.md`, subsequent-release path,
executed by `release-script-b2.sh` (predecessor `31b78f81`, `MEMBER_VERSION=0.3.0`, `PROBE_PR=164`),
retained in the evidence directory and in `~/.local/state/brain-v42-delivery/handoff-2026-09-21/`.

This release runs **no migration**. Migration 055 was applied to production earlier the same day,
inside the merge window and before any writer could reach the new tables — the ADR #17 order, and
the inverse of the 049 incident. What this release does is close the gap that order deliberately
opened: production ran `31b78f81` (shipped head 054) against a schema already at 055 for roughly
one hour, with `knowledge_fact_definitions`, `knowledge_claims` and `knowledge_claim_verdicts`
present and empty because nothing deployed could write to them.

It carries four merged pull requests, 24 non-merge commits:

| PR | What it ships |
| --- | --- |
| **#162** | an undeclared killswitch key renders as undeclared, not as an unreadable typo |
| **#165** | the Quick start's first command could not work (`pip install -e ".[dev]"` against a uv workspace member), plus three shipped capabilities the README never named |
| **#163** | migration 055 — the three claim ledgers, their SQL-enforced immutability, the deterministic `knowledge_claim_current` view, and the fact-definition registrar that is its only writer |
| **#164** | claims become writable: the five knowledge writers accept declared claims, `brain_update` replaces them atomically, and the recovery contract v12 covers head 055 |

## Release record

The release is `93d8b83207a9251a454a0d0a542859107a251aab`, the merge commit of pull request #164
into `main`. Its predecessor was `31b78f818bbba2ac778be4f145f86783719ff03d`, measured on the eight
`ExecStart` before the window; all eight writers were on it at build time.

The merged `main` revision was verified on the host before the build: `ruff check`,
`ruff format --check` (1044 files) and `mypy src/ packages/headless-agents/src/` (298 files) clean,
then 11 004 unit tests passed with **one failure** —
`test_documented_migration_head_matches_repository`, which pins the repository head in CLAUDE.md
only `if CLAUDE:`. CLAUDE.md is gitignored, so that branch never runs in CI and the nine CI jobs
were green on all three pull requests; on the host the file existed and still said 054. Correcting
it to 055 turned the contract green (111 passed). The CI-tested tree was confirmed identical to
`main`: `git diff 18a69317 93d8b832` is empty.

| Input | Measured value |
| --- | --- |
| Version in `pyproject.toml` and `uv.lock` | `0.6.0`; `headless-agents` `0.3.0` — both unchanged |
| Build interpreter | `/opt/red-python/cpython-3.12.12/bin/python3.12` |
| Working tree | clean at `SOURCE_SHA` |
| Repository Alembic head | `055` — the release's own change of state |
| Production Alembic head | `055` — already there, unchanged by this release |
| Drop-ins | all eight rendered `90-immutable-release.conf` and the observer unit byte-identical to the live ones with the SHA masked, including `brain-mcp-http`'s `BRAIN_FACTS_LIVE_RELEASE_IDENTITY` line, which was the one difference of the previous release and is now on both sides |
| `zz-` drop-ins at build and at cutover | none |

Artifact digests (SHA-256):

| Artifact | Digest |
| --- | --- |
| `brain_v42-0.6.0-py3-none-any.whl` | `1dee29b477a35d7fad9d792d2717e6a51230ceb432c2911736cfbf4c20f6e331` |
| `headless_agents-0.3.0-py3-none-any.whl` | `12ac708ca9860f33498992d541f8cbf2ac19e88a2f31cfeefbea408391309618` |
| `brain-v42.tar.gz` (source archive) | `b14694e14560b9cc6b1aeb29931f0aab600d19f09474b912d0e8e1268d2dc5a6` |
| `delivery-release.json` (manifest) | `73296f9ba3fa4bf0ad23da47edd58b312368e0e25376bd95b3d7975535aab659` |

The manifest binds 343 package payload files, 70 source payload files and the workspace member's
15. The smoke, run outside the checkout, asserted that `brain_v42` and `headless_agents` resolve
inside the release venv, that the installed versions are `0.6.0` and `0.3.0`, and that
`shipped_alembic_head()` returns **`055`**.

## Build and cutover

Build: 17:26:58 → 17:27:04 UTC, production untouched.

Cutover: 17:27:14 → 17:27:24 UTC (19:27 CEST — outside the 06:00–09:00 window, outside 13:00, and
`brain-v42-dream` inactive with its next trigger ten hours away). Four active triggers frozen
(`brain-mcp-http-watchdog.timer`, `brain-mcp-reaper.timer`, `brain-v42-dream.timer`,
`brain-v42-model-liveness.timer`), the eight writers stopped and each confirmed at `MainPID=0`,
the reviewed drop-ins published, `brain-mcp-http` and `brain-metrics` started dormant, dormant
preflight `status=ok` / `source_sha=93d8b832…` / `schema_revision=055`, triggers restored with the
watchdog last, and the observer restarted because it was active before the window.
`unit-state.before` and `unit-state.after` are byte-identical (SHA-256 `8f3210aff5a830c2…` on both
sides). All eight writers measured on the new SHA. `/health` of the dormant MCP reported
`version 0.6.0`, `alembic_head 055`.

`brain-v42-model-liveness.service` was `failed` before the window and is `failed` after: a
pre-existing state this release neither caused nor repaired.

## Acceptance

**The registrar wrote, for the first time in production.** The journal of `brain-mcp-http`
(PID 2764389) at start: `facts.registry_frozen facts=['graph_projection_lag', 'alembic_head',
'live_release_sha', 'alembic_head_shipped', 'dream_killswitches_declared', 'dream_last_night']`
with no identity warning, then six `facts.definition_registered` lines, each with its digest and
`definition_version=1`. `knowledge_fact_definitions` went from 0 rows to 6.

**The facts measured against their declared identities**, all `status=measured`,
`source_kind=probe`:

| `brain_fact_get(name, max_age_seconds=0)` | Result |
| --- | --- |
| `live_release_sha` | `release_sha=93d8b832…`, `package_version=0.6.0`, 0 ms |
| `alembic_head` | `revision=055`, production identity `7612696091383607335 / brain / 172.31.0.4 / 5432`, 2 ms |
| `alembic_head_shipped` | `revision=055` from the strict read of the shipped migrations, 6 ms |
| `graph_projection_lag` | `healthy=true`, generation 99 armed, lease held, 0 pending, lag 0 s |

`alembic_head` and `alembic_head_shipped` now carry the **same digest**
(`9064095f3cea18f7…`), because they resolve to the same value against two independently declared
targets. That equality is the release's purpose, stated in one number.

**The claim write path works end to end.** A `brain_learn` call carrying one declared claim
returned `claims:1 recorded (declared; verification arrives with the verdict path)` and left one
row in `knowledge_claims`: `fact_name=alembic_head_shipped`, `target=live_release`,
`definition_version=1`, `expected={"op":"eq","path":"/revision","value":"055"}` equal to
`expected_resolved`, `provenance=declared`. `knowledge_claim_verdicts` stays at 0 and the
`knowledge_claim_current` view projects 0 — correct: the verdict path is lot B slice 3 and does
not exist yet, so no claim can be anything but `declared`.

One malformed input was refused before persistence, by structural validation rather than by the
database: an `expected.path` of `revision` instead of `/revision` raised `path rule requires a
JSON pointer starting with /`. Leaf-only validation is live.

**The immutability triggers are armed in production**, verified inside a rolled-back transaction:
`DELETE` raises `knowledge_claims is append-only: DELETE refused`, `UPDATE` raises
`knowledge_claims only permits retired_at NULL -> non-NULL; UPDATE refused`. Both come from the
PL/pgSQL gates, not from a GRANT — which matters, because the `brain` role is superuser and the
GRANTs of §6.1/§6.2 are inert today (learning `d857f21c`).

## Rollback

`PROBE_PR=164 release-script-b2.sh rollback 93d8b83207a9251a454a0d0a542859107a251aab 20260921T173000Z`:
republishes the backed-up `31b78f81` drop-ins and restarts `brain-mcp-http` and `brain-metrics`.

It is a **code-only** rollback, and it reopens the gap this release closed: the predecessor ships
head 054 while production stays at 055, because nothing here rolls a migration back. The claim
rows already written survive — they are append-only by trigger — and become unreachable by any
deployed writer until a release ships 055 again. That is the safe direction, and it is the state
production held for the hour before this window.
