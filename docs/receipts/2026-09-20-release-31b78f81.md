# Immutable release 31b78f81 — production receipt

**Status: FINAL — the release is live; all six facts measured against their declared
identities within a minute of the window.**
Window `20260920T105548Z`. Evidence directory
`~/.local/state/brain-v42-delivery/31b78f818bbba2ac778be4f145f86783719ff03d/20260920T105548Z`.
Procedure: `docs/runbooks/2026-09-07-observable-delivery-workflows.md`, subsequent-release path
(no schema change), executed by the 2026-09-20 script `release-script-slice2.sh` (predecessor
`e3777f59`, `MEMBER_VERSION=0.3.0`, `PROBE_PR=161`, and the one rendering change the runbook now
documents: `brain-mcp-http`'s `90-immutable-release.conf` carries
`BRAIN_FACTS_LIVE_RELEASE_IDENTITY`), retained in the evidence directory and in
`~/.local/state/brain-v42-delivery/handoff-2026-09-20/`.

This release carries no schema change. It makes pull request **#161** (`feat/facts-slice-2`, lot A
slice 2 of ADR #27) reachable by the eight systemd writers: five facts on three verified targets
— `alembic_head`, `live_release_sha`, `alembic_head_shipped`, `dream_killswitches_declared`,
`dream_last_night` — with the `ReleaseIdentity` and `HostIdentity` declarations, the fd-based
host reader, and the briefing's `Schéma` line bound to the `alembic_head` fact. It is the second
release of the day: `e3777f59` (10:03 UTC, facts slice 1 and the Dream dead-link fallthrough)
ran for 53 minutes.

## Release record

The release is `31b78f818bbba2ac778be4f145f86783719ff03d`, the merge commit of pull request #161
into `main`. Its predecessor was `e3777f5912886df19e5a5ddccefe6b67536db364`, measured on the
eight `ExecStart` after the 10:03 UTC cutover; all eight writers were on it at build time.

The merged `main` revision was verified before the pull request: 10 796 unit tests passed
(120 skipped), `tests/integration/facts` 19 passed against `brain_test`, `ruff check`,
`ruff format --check` and `mypy src/ packages/headless-agents/src/` clean, the nine CI jobs green,
and an independent review of the whole branch (red-reviewer, opus, tests re-read) whose four
should-fixes were closed in the last commit with thirteen new tests red before green.
Production was at 054 throughout.

| Input | Measured value |
| --- | --- |
| Version in `pyproject.toml` and `uv.lock` | `0.6.0`; `headless-agents` `0.3.0` |
| Build interpreter | `/opt/red-python/cpython-3.12.12/bin/python3.12` |
| Working tree | clean at `SOURCE_SHA` |
| Repository Alembic head | `054` — unchanged |
| Drop-ins | seven rendered `90-immutable-release.conf` and the observer unit byte-identical to the live ones with the SHA masked; `brain-mcp-http`'s differs by exactly one line, `Environment='BRAIN_FACTS_LIVE_RELEASE_IDENTITY={"release_sha":"<sha>","package_version":"0.6.0"}'`, read and expected |
| `zz-` drop-ins at build and at cutover | none |

Artifact digests (SHA-256):

| Artifact | Digest |
| --- | --- |
| `brain_v42-0.6.0-py3-none-any.whl` | `54465322b9a3a678c3d03d2f8c7693fc63441fc5cdd083370a2ec0c4de30c8b9` |
| `headless_agents-0.3.0-py3-none-any.whl` | `3500ee36b4ff2a8e44f79cf9a593c9d3951ae0b48c7294ad60ad224857a7709b` (unchanged since `e3777f59`) |
| `brain-v42.tar.gz` (source archive) | `69b9ab7012d7800b4a56458227b13b07baaaba0cd2bebf96ad231a8b71725f3d` |
| `delivery-release.json` (manifest) | `32227cd0a8265dc9325cc8df7db085a7ae82fc43f3b3814e4b79816f2880f9f1` |

The smoke, run outside the checkout, asserted that `brain_v42` and `headless_agents` resolve
inside the release venv, that the installed versions are `0.6.0` and `0.3.0`, and that
`shipped_alembic_head()` returns `054`.

## Build and cutover

Build: 10:55:49 → 10:55:54 UTC, production untouched.

Cutover: 10:56:08 → 10:56:17 UTC (12:56 CEST, outside the 06:00–09:00 window) — quiesce of the
four active triggers and the eight writers, publication of the reviewed drop-ins, dormant start of
`brain-mcp-http` and `brain-metrics`, dormant preflight (`status=ok`, `source_sha=31b78f81…`,
`schema_revision=054`), triggers restored with the watchdog last, observer restarted because it
was active before the window. `unit-state.before` and `unit-state.after` are byte-identical
(SHA-256 `da3142b59fc1fb72…` on both sides). All eight writers measured on the new SHA;
`/health` of the dormant MCP reported `version 0.6.0`, `alembic_head 054`.

## Acceptance: the six facts against their declared identities

The journal of `brain-mcp-http` (PID 1277051) at start: `facts.registry_frozen
facts=['graph_projection_lag', 'alembic_head', 'live_release_sha', 'alembic_head_shipped',
'dream_killswitches_declared', 'dream_last_night']`, no identity warning. The live process
environment carries the three declarations (`BRAIN_FACTS_PRODUCTION_IDENTITY`,
`BRAIN_FACTS_HOST_IDENTITY` from `92-facts-identity.conf`; `BRAIN_FACTS_LIVE_RELEASE_IDENTITY`
from the release's own drop-in).

| `brain_fact_get(name, max_age_seconds=0)` | Result (all `status=measured`, `source_kind=probe`) |
| --- | --- |
| `live_release_sha` | source and value `release_sha=31b78f81…`, `package_version=0.6.0`, 0 ms |
| `alembic_head` | `revision=054`, production identity `7612696091383607335 / brain / 172.31.0.4 / 5432`, 2 ms |
| `alembic_head_shipped` | `revision=054` from the strict read of the shipped migrations, 6 ms |
| `dream_killswitches_declared` | host identity `hawixspcicube`; raw strings `promote=true, reorg=true, reorg_dry=false, extract=true, extract_dry=false, sweep=true, sweep_dry=false, roadmap="", roadmap_dry=""`, `file_mtime_epoch=1789481423` (the drop-in of 2026-09-15 16:10 CEST) |
| `dream_last_night` | night `2026-09-20`: `rows=62, done=62, fail=0, timeout=0, partial=0, other=0, wet=62, dry=0, projects=10` — the `'*'` sentinel of the global phases excluded, as designed |
| `brain_fact_list()` | six descriptors in catalogue order; `dream_last_night` is `briefing=false` |

One finding from the canary, corrected in the same pull request as this receipt: the ROADMAP
keys are absent from the drop-in since the phase retired on 2026-09-10, so their raw value is
`""`; the first renderer read that as an illegible typo (`ROADMAP off '' (illisible → off)`).
It now says `ROADMAP off (non déclaré)`. The briefing lines themselves (`Schéma`, `Release
vivante`, `Tête Alembic livrée`, `Killswitches déclarés`) are rendered by the next explicit
session lifecycle command; the lifecycle stays a user command.

## Rollback

`release-script-slice2.sh rollback 31b78f818bbba2ac778be4f145f86783719ff03d 20260920T105548Z`:
republishes the backed-up `e3777f59` drop-ins (without the live_release declaration) and
restarts `brain-mcp-http` and `brain-metrics`. On the predecessor the `live_release` facts do not
exist; the two others keep their declarations.
