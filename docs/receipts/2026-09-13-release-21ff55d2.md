# Immutable release 21ff55d2 — production receipt

**Status: FINAL — the release is live; both on-demand canaries required by contract afd56820 ran on it before its first scheduled Dream night (2026-09-13 06:00 CEST).**
Window `20260912T233254Z`. Evidence directory
`~/.local/state/brain-v42-delivery/21ff55d24c4b96db2560bab2382d51170826fb6e/20260912T233254Z`.
Procedure: `docs/runbooks/2026-09-07-observable-delivery-workflows.md`, subsequent-release path
(no schema change; private-configuration, recovery-proof, migration and activation blocks not
run), executed by the same three-mode script as releases a20265ac and c7ae408a, retained as
`release-script.sh` in the evidence directory.

This release carries no schema change. It makes pull requests #140 (the production receipt of
release c7ae408a) and #141 reachable by the eight systemd writers. PR #141 is lot 2 of the agent
runtime extraction, delivered under Brain ticket `afd56820` with delivery contract revision 2: the
provider chain and the execution of one Dream phase move from the bash of `scripts/dream.sh` to
`brain_v42.agents`, with identical log lines and identical runner and parser argv.

## Release record

The release is `21ff55d24c4b96db2560bab2382d51170826fb6e`, the merge commit of pull request #141
into `main`. Its predecessor was `c7ae408a8ef6492c6dd5ccd005bf40e04972b530`.

The merged `main` revision was verified on the host before the build: 10193 unit tests passed
(107 skipped), `ruff check`, `ruff format --check`, `mypy src/` and `bash -n scripts/dream.sh`
clean, 396 dream and delivery integration tests passed against `brain_test` at head 053
(484 deselected). Production was at 053 throughout.

| Input | Measured value |
| --- | --- |
| Version in `pyproject.toml` and `uv.lock` | `0.5.0` |
| Build interpreter | `/opt/red-python/cpython-3.12.12/bin/python3.12`, 3.12.12, regular file; `venv/pyvenv.cfg` hashed in the manifest |
| Working tree | clean at `SOURCE_SHA` |
| Repository Alembic head | `053` — unchanged |
| Manifest payload | 312 wheel files mapped to the source archive (307 in c7ae408a; the five new `brain_v42/agents/` modules of lot 2), 69 retained source files |

Artifact digests (SHA-256):

| Artifact | Digest |
| --- | --- |
| `brain_v42-0.5.0-py3-none-any.whl` | `52eadc746ead85db036e6708864baddd52c2d3c531400e62f373e7b7d47426b3` |
| `brain-v42.tar.gz` (source archive) | `c2e28df4819535b23c53fafed9f2d5a60ff64bcdd784ec1d5c8416486eda8a6d` |
| `delivery-release.json` (manifest) | `274eab999c9e24783a82c023e0525abc3148dd1b2814b32c8abeaf2a6d9dea1b` |

The smoke, run outside the checkout, asserted that `brain_v42` resolves inside the release venv,
that the installed version is `0.5.0`, that `shipped_alembic_head()` returns `053`, and that the
observer, `check_delivery_deployment.py` and `verify_delivery_canary.py` answer `--help`.

## Drop-in review

The eight rendered `90-immutable-release.conf` drop-ins and the rendered observer unit were
compared against the live ones with the release SHA masked on both sides: all byte-identical
under that mask. `PATH_TAIL`, the reaper's `--max-age-hours 48` and `WorkingDirectory=/home/hawixs`
were read from the live configuration. `91-delivery-mode.conf` was rewritten with identical
content. A pre-window guard confirmed all eight writers on the predecessor SHA. The preflight
configuration names pull request #141 as the dormant probe. No Dream drop-in was changed by this
release: lot 2 reads the same environment variables as the bash it replaces, and its only new
variable, `BRAIN_DREAM_AGY_GUARD_PATH`, is set by `run_phase` itself from the release tree.

## Cutover

The window ran from 23:33:19 to 23:33:29 UTC on 2026-09-12 (01:33 CEST on 2026-09-13). Four
triggers were active and were recorded before being stopped: `brain-mcp-http-watchdog.timer`,
`brain-mcp-reaper.timer`, `brain-v42-dream.timer`, `brain-v42-model-liveness.timer`
(`triggers.active.before`, SHA-256
`6f66cf7839efead91d52bfe0d2ec77aaf673570b81309552eea750e3885276f3`, the same list and digest as
release c7ae408a). Three writers were active and were stopped: `brain-mcp-http.service`,
`brain-metrics.service`, `brain-v42-delivery-observer.service`. Every writer was asserted at
`MainPID=0` before publication.

MCP health in dormant mode (SHA-256 `6bdc0bfb5bba4bcd4980d396a94b75dc4fd5da822455a42c55e731bdf4e43690`):

```json
{"status":"ok","version":"0.5.0","alembic_head":"053","pool":{"size":20,"checked_out":0}}
```

The dormant preflight returned, verbatim (SHA-256 `2aa1aea0f2101d40b4064da979893fad9388d9637407ef6ef23e1e007dab8df2`):

```json
{"schema_revision":"053","source_sha":"21ff55d24c4b96db2560bab2382d51170826fb6e","status":"ok","timestamp":"1789256006"}
```

Triggers were restored with the watchdog last, and the observer was started again because it was
active before the window. `unit-state.before` and `unit-state.after` are byte-identical (SHA-256
`da3142b59fc1fb72df3eddb515e4b31a5569f6e65258dd4d5581d966158e5bb0` on both sides): enablement
and activity of the eight writers and five triggers unchanged. All eight writers were confirmed
on the new SHA.

## On-demand DRY canary (acceptance criterion 8a of contract afd56820)

A temporary drop-in (`BRAIN_DREAM_PROJECT_POOL=brain-v42`, `DRY_RUN=true`, extract, sweep and
reorg forced dry) was installed, the Dream unit started, the running process's environment
verified to carry those five values, and the drop-in removed with a `daemon-reload` while the run
was in its first phase, so the unit's environment for the 06:00 night (ten-project pool, WET) was
restored before the canary finished. The run lasted from 01:33:47 to 01:47:32 CEST.

| Phase | Provider / model | Outcome |
| --- | --- | --- |
| scan, clean, connect | codex, `gpt-5.6-luna` / high | done |
| synth, promote, reorg | codex, `gpt-6-astra` / max | done |
| extract | NVIDIA `nemotron-3-super-120b-a12b`, dry | done |
| sweep | none, dry | done |

`Dream finished: 8/8 phases OK`; `dream_runs` coverage `expected 8 written 8 skipped 0 declared 0
writefail 0 silent 0 extra 0 mismatch 0`; reconciliation `phases_ok=8 pairs_written=8 gap=0`
(no other run had written on that date yet); `REORG VALIDATE: OK — mode=dry-run updated=0
archived=0`; `CONNECT VALIDATE: OK`. No `FALLBACK` line. The fourteen `START`/`DONE` lines are
byte-identical to those of the c7ae408a canary of 2026-09-12 once the `[HH:MM:SS]` prefix is
masked (`diff` empty) — the format equivalence the contract asks for, now produced by
`brain_v42.agents.phase` instead of the bash `run_phase`. The eight `dream_runs` rows carry the
models above (`canary-dry-dream_runs.txt`); the log is retained as `canary-dry-2026-09-13.log`.

## On-demand forced-fallback canary (acceptance criterion 8b of contract afd56820)

A second temporary drop-in was installed with the same five dry values plus
`BRAIN_DREAM_CODEX_BIN=~/.local/share/brain-v42/canary/codex-fallback-stub`, a script that answers
`Logged in using ChatGPT` to `login status` (so the dream.sh preflight keeps codex in the chain)
and refuses every other invocation without producing an event or a tool call. The running
process's environment was verified to carry the six values and the drop-in was removed with a
`daemon-reload` during the first phase. The run lasted from 01:48:17 to 01:58:44 CEST.

| Phase | codex attempt | Fallback | agy model | Outcome |
| --- | --- | --- | --- | --- |
| scan | `FAIL scan (exit=3 — aucun appel d'outil Brain abouti)` | `FALLBACK brain-v42/scan — … bascule vers agy` | `gemini-3.8-flash-high` | done |
| clean | same, exit 3 | `FALLBACK brain-v42/clean` | `gemini-3.8-flash-high` | done |
| connect | same, exit 3 | `FALLBACK brain-v42/connect` | `gemini-3.8-flash-high` | done |
| synth | same, exit 3 | `FALLBACK brain-v42/synth` | `gemini-3.1-pro-high` | done |
| promote | same, exit 3 | `FALLBACK brain-v42/promote` | `gemini-3.1-pro-high` | done |
| reorg | same, exit 3 | `FALLBACK brain-v42/reorg` | `gemini-3.1-pro-high` | done |
| extract, sweep | not agent phases | — | — | done, dry |

Every agent phase was first started on codex (`START … provider=codex`), failed on the stub within
two seconds with exit code 3, logged one `FALLBACK codex→agy` line and was restarted on agy; all
six finished `DONE` on agy. `dream_runs` carries a `fail` row with the codex model and a `done`
row with the agy model for each of the six phases (`canary-forced-fallback-dream_runs.txt`).
The night summary reads `Dream finished: 8/8 phases OK, 6 repliées sur un secours (brain-v42/scan
brain-v42/clean brain-v42/connect brain-v42/synth brain-v42/promote brain-v42/reorg)`: that list
is what `dream.sh` now reads from the `--result-json` of `brain_v42.agents.run_phase_chain`, the
seam lot 2 introduced. Coverage `expected 8 written 8 mismatch 0`, reconciliation `pairs_written=8
gap=0`, `CONNECT VALIDATE: OK`, `REORG VALIDATE: OK — mode=dry-run updated=0 archived=0`. The log
is retained as `canary-forced-fallback-2026-09-13.log`.

One observation outside the contract: the dry `extract` of the DRY canary scanned two tickets and
staged 14 proposals (ids 423–436), exactly as the c7ae408a canary had done the day before (ids
409–422); the forced-fallback run then found no pending ticket and wrote a `dream_runs` row with an
empty model. This is the pre-existing behaviour of the dry extract, untouched by lot 2.

## Delivery workflow trace

Contract revision 2 set at 18:10:10 UTC on 2026-09-12 (revision 2 corrects the forced-fallback
mechanism: a stub that passes `login status`, not `/bin/false`, since the dream.sh preflight
would otherwise drop codex from the chain before any phase runs). Binding `21f9329e` on PR #141,
deliverable `agents-provider-chain`, observed with head `359965f5` on base `c0e48966`; nine
protected checks green; merged at 18:41:29 UTC; integration recorded at 18:42:13 UTC with
`integration_sha 21ff55d2` and integration receipt `c4dabbbe` issued; the observer re-confirmed
the binding every minute afterwards (last success 23:29:18 UTC before the window).
Explicit acceptance at 23:59:30 UTC on 2026-09-12, after both canaries, issued the fulfilled receipt
`eff2fb53` (workflow version 709, delivery digest
`6fcbe531bc31f5ef256ecbed75339c6e14a26645e2c6d3bd22747eed09710e42`, upstream receipt `77dc84f2`
of lot 1); the ticket was then resolved and closed. The five pinned repository documents and the
two pinned Brain entities (decision `a06f57be`, runbook `ed9e0e31`) were re-collected by the
observer at 23:58 UTC with digests equal to the contract's.

## Rollback

`c7ae408a8ef6492c6dd5ccd005bf40e04972b530` remains retained with its dormant preflight file and is
the forward-rollback target; it was built on `/opt/red-python`. The backed-up drop-ins of the
predecessor sit in `systemd-before/` of the evidence directory for the script's `rollback` mode.
