# Immutable release 84138170 — production receipt

**Status: FINAL — the release is live on the eight writers; the on-demand Dream canary required
by contract b2a2d1a5 ran on it before its first scheduled Dream night (2026-09-15 06:00 CEST);
tag `v0.6.0` is published and the workspace member installs from it.**
Window `20260914T113846Z`. Evidence directory
`~/.local/state/brain-v42-delivery/84138170af790f80c05f4b263dee6c206ec0ea7c/20260914T113846Z`.
Procedure: `docs/runbooks/2026-09-07-observable-delivery-workflows.md`, subsequent-release path
(no schema change; private-configuration, recovery-proof, migration and activation blocks not
run), executed by the three-mode script of releases a20265ac, c7ae408a and 21ff55d2 adapted to
the two-wheel build this release introduces, retained as `release-script.sh` in the evidence
directory.

This release carries no schema change. It makes pull request #143 reachable by the eight systemd
writers. PR #143 is Brain ticket `b2a2d1a5` (a request from the ReD root, decision `3c5c56e1`)
under delivery contract revision 1, deliverable `headless-agents`: the generic part of
`brain_v42.agents` becomes the `headless-agents` uv workspace member (`packages/headless-agents/`,
import `headless_agents`, version `0.1.0`, dependency `pydantic` only), driven by a caller-supplied
`CapabilityProfile`; `brain_v42.agents` stays as the Dream's adapter over it, with argv, child
environment, ephemeral HOME files, log lines and exit codes unchanged under the golden fixtures.
The version moves from `0.5.0` to `0.6.0`.

## Release record

The release is `84138170af790f80c05f4b263dee6c206ec0ea7c`, the merge commit of pull request #143
into `main`. Its predecessor was `21ff55d24c4b96db2560bab2382d51170826fb6e`.

The merged `main` revision has the same tree as the pull request head `4f72da1c` (`git diff`
empty), on which the nine protected checks ran green; on the host, `ruff check`,
`ruff format --check`, `mypy src/ packages/headless-agents/src` and the agents, delivery and
runner unit suites (355 tests) were rerun on the merge commit. Before the merge the full unit suite
had passed on the branch head (10343 passed, 107 skipped) and the dream and delivery integration
tests against `brain_test` at 053 (398 passed once; on the rerun one timing-dependent observer
test, untouched by the branch, failed once and passed again). Production was at 053 throughout.

| Input | Measured value |
| --- | --- |
| Version in `pyproject.toml` and `uv.lock` | `0.6.0`; member `packages/headless-agents/pyproject.toml` `0.1.0` |
| Build interpreter | `/opt/red-python/cpython-3.12.12/bin/python3.12`, 3.12.12, regular file; `venv/pyvenv.cfg` hashed in the manifest |
| Working tree | clean at `SOURCE_SHA` |
| Repository Alembic head | `053` — unchanged |
| Third-party requirements | `uv export --locked --no-dev --no-emit-workspace` (the member is not an emitted requirement), `uv pip sync --require-hashes` |
| Manifest payload | 312 `brain_v42/` wheel files mapped to the source archive, 69 retained source files, and one `workspace_wheels` entry: `headless_agents`, member `packages/headless-agents`, 14 wheel files mapped to `brain-v42/packages/headless-agents/src/` |

Artifact digests (SHA-256):

| Artifact | Digest |
| --- | --- |
| `brain_v42-0.6.0-py3-none-any.whl` | `edc3e23495e9c1bca2eb9daff18a29a7badf4fe41ae6ff9bc7a94a723f583c2c` |
| `headless_agents-0.1.0-py3-none-any.whl` | `3d3e5079bc44e4708efb731ac247df174d7d2d9241e78596e34a1c3347e6e064` |
| `brain-v42.tar.gz` (source archive) | `49099168d91c0d318d35b2a87ef6e3e3f1ce1498ed601e2ed0809bd81149415c` |
| `delivery-release.json` (manifest) | `99bb617a56183907612f34242f94b8969867f5bba781778f4d545a2de03bdf3a` |

Both wheels were installed into the release venv with `--no-deps`. The smoke, run outside the
checkout, asserted that `brain_v42` and `headless_agents` both resolve inside the release venv,
that the installed versions are `0.6.0` and `0.1.0`, that `shipped_alembic_head()` returns
`053`, and that the observer, `check_delivery_deployment.py` and `verify_delivery_canary.py`
answer `--help`.

## Drop-in review

The eight rendered `90-immutable-release.conf` drop-ins and the rendered observer unit were
compared against the live ones with the release SHA masked on both sides: all byte-identical
under that mask. A pre-window guard confirmed all eight writers on the predecessor SHA. The
preflight configuration names pull request #143 as the dormant probe. No Dream drop-in was
changed by this release: the Dream adapters read the same environment variables as before and
build the capability profile from them.

## Cutover

The window ran from 11:51:49 to 11:51:58 UTC on 2026-09-14 (13:51 CEST). Four triggers were
active and were recorded before being stopped: `brain-mcp-http-watchdog.timer`,
`brain-mcp-reaper.timer`, `brain-v42-dream.timer`, `brain-v42-model-liveness.timer`
(`triggers.active.before`, SHA-256
`6f66cf7839efead91d52bfe0d2ec77aaf673570b81309552eea750e3885276f3`, the same list and digest as
the two previous releases). Three writers were active and were stopped: `brain-mcp-http.service`,
`brain-metrics.service`, `brain-v42-delivery-observer.service`; `brain-v42-dream.service` and
`brain-v42-model-liveness.service` were in state `failed` from the night before (Codex usage limit,
see below) and stayed so, as `unit-state` records on both sides. Every writer was asserted at
`MainPID=0` before publication.

MCP health in dormant mode (SHA-256 `e381a39bcf0173e70b9f77bff2f7123fcb01e7dd4186860840105c729971c66a`):

```json
{"status":"ok","version":"0.6.0","alembic_head":"053","pool":{"size":20,"checked_out":0}}
```

The dormant preflight returned, verbatim (SHA-256 `23284ade1d91d9968afc35542e72e086d5549a31f2fbb079711f8e5ba02901b2`):

```json
{"schema_revision":"053","source_sha":"84138170af790f80c05f4b263dee6c206ec0ea7c","status":"ok","timestamp":"1789386716"}
```

This is the first preflight to attest a workspace member: the `check_delivery_deployment.py` of
this release validated the `headless_agents` wheel under `workspace_wheels` (archive, wheel and
installed digests per file, version agreement between the wheel's `METADATA` and the member's
`pyproject.toml`, no installed file beyond the declared ones) and read the main wheel's
`Requires-Dist` to demand the member's presence.

Triggers were restored with the watchdog last, and the observer was started again because it was
active before the window. `unit-state.before` and `unit-state.after` are byte-identical (SHA-256
`3ccd14218adb0955ed93e4241d549ca878026e1459b6f787137c12f36cd7b085` on both sides). All eight
writers were confirmed on the new SHA.

## On-demand DRY canary (acceptance criterion 6 of contract b2a2d1a5)

The temporary drop-in of the previous canaries (`BRAIN_DREAM_PROJECT_POOL=brain-v42`,
`DRY_RUN=true`, extract, sweep and reorg forced dry) was installed, the Dream unit started at
13:52 CEST, the running process's environment verified to carry those five values and its command
line to name `releases/84138170…/brain-v42/scripts/dream.sh brain-v42`, and the drop-in removed
with a `daemon-reload` before the first phase started. The run lasted from 13:52:52 to 14:01:58
CEST.

The Codex subscription was at its usage limit throughout (probed before the run with a one-word
`codex exec`: `You've hit your usage limit … try again at Sep 19th`; the 06:00 night of the same
day had already fallen back to agy on 23 phases from 07:32). The canary therefore ran as a
**real fallback canary**, without the stub of 2026-09-13: every agent phase was started on codex,
failed within seconds with exit code 3 (no Brain tool call completed, hence replayable), logged
one `FALLBACK codex→agy` line and finished `DONE` on agy.

| Phase | codex attempt | Fallback | agy model | Outcome |
| --- | --- | --- | --- | --- |
| scan | `FAIL  scan (exit=3 — aucun appel d'outil Brain abouti)` | `FALLBACK brain-v42/scan — … bascule vers agy` | `gemini-3.8-flash-high` | done |
| clean | same, exit 3 | `FALLBACK brain-v42/clean` | `gemini-3.8-flash-high` | done |
| connect | same, exit 3 | `FALLBACK brain-v42/connect` | `gemini-3.8-flash-high` | done |
| synth | same, exit 3 | `FALLBACK brain-v42/synth` | `gemini-3.1-pro-high` | done |
| promote | same, exit 3 | `FALLBACK brain-v42/promote` | `gemini-3.1-pro-high` | done |
| reorg | same, exit 3 | `FALLBACK brain-v42/reorg` | `gemini-3.1-pro-high` | done |
| extract, sweep | not agent phases | — | — | done, dry |

`Dream finished: 8/8 phases OK, 6 repliées sur un secours (brain-v42/scan brain-v42/clean
brain-v42/connect brain-v42/synth brain-v42/promote brain-v42/reorg)`; `dream_runs` coverage
`expected 8 written 8 skipped 0 declared 0 writefail 0 silent 0 extra 54 mismatch 0` (the 54
extra rows are the night's other projects); `CONNECT VALIDATE: OK — STEP_A.errors=0
STEP_B.errors=0`; `REORG VALIDATE: OK — mode=dry-run updated=0 archived=0`. The thirty phase lines
(`START` codex, `FAIL`, `FALLBACK`, `START` agy, `DONE` for each of the six phases) are
byte-identical to those of the forced-fallback canary of 2026-09-13 once the `[HH:MM:SS]` prefix is
masked (`diff` empty; `canary-2026-09-14.phase-lines` against
`canary-reference-2026-09-13-forced-fallback.phase-lines`). `dream_runs` carries a `fail` row with
the codex model and a `done` row with the agy model for each phase, all with `phase_dry_run=t`
(`canary-dream_runs.txt`). No learning, decision or snippet was created during the run. The log
is retained as `canary-dry-fallback-2026-09-14.log` (SHA-256
`a19354138c98dafe1ae19111e5970aa55e354385cf5767029191f64e5ff72aa0`).

What this canary proves about the extraction: the codex phases went through
`brain_v42.agents.providers.codex` over `headless_agents.providers.codex` (launch, event-stream
validation, exit code 3), the chain through `headless_agents.chain.run_chain` with the Dream's
`FALLBACK` lines, and the agy phases through an ephemeral HOME composed by
`headless_agents.sandbox.build_ephemeral_home` from the Dream's profile (scoped bearer written
literally, `dream-phase-guard` hook, symlinked credentials) — the three code paths the split
touched most.

## Tag and consumption (acceptance criteria 4 and 7)

Tag `v0.6.0` was created on `84138170` and pushed; the `release.yml` rail (hosted runner) ran
green and published the GitHub release with three assets: `brain_v42-0.6.0-py3-none-any.whl`,
`brain_v42-0.6.0.tar.gz` and `headless_agents-0.1.0-py3-none-any.whl`.

From a fresh `uv init` project with an empty virtual environment:

```sh
uv add "headless-agents @ git+https://github.com/hawkixs/brain-v42.git@v0.6.0#subdirectory=packages/headless-agents"
```

resolved to `rev=v0.6.0#84138170af790f80c05f4b263dee6c206ec0ea7c` in the consumer's `uv.lock` and
installed `headless_agents`, `pydantic`, `pydantic_core`, `annotated_types`,
`typing_extensions` and `typing_inspection` — nothing else. Importing every runtime module in a
fresh `-I` interpreter took 0.148 s cold and 0.053 s warm, with none of `brain_v42`,
`sqlalchemy`, `asyncpg`, `neo4j`, `pgvector`, `fastmcp` or `uvicorn` in `sys.modules`. Measured
before the split, importing `brain_v42.agents.providers.codex` loaded all five heavy
distributions in about one second.

## Delivery workflow trace

Contract revision 1 set at 10:09:57 UTC on 2026-09-14 by the requester (`red`); ticket started
in the same minute. Binding `d962c0d5` on PR #143, deliverable `headless-agents`, observed with
head `4f72da1c` on base `e3a289c1`; nine protected checks green (the first run had
`security-pip-audit` red on the `-e ./packages/headless-agents` line `uv export --no-emit-project`
emits for a workspace member, fixed in the last commit of the branch); merged at 11:37 UTC;
integration recorded with `integration_sha 84138170` and integration receipt `bd764cc9` issued at
11:38:02 UTC (contract digest `5740bd8d…`, delivery digest `2ba45d4b…`). Explicit acceptance and
the ticket's resolution follow this receipt.

## Rollback

`21ff55d24c4b96db2560bab2382d51170826fb6e` remains retained with its dormant preflight file and is
the forward-rollback target; it was built on `/opt/red-python` and carries a single wheel, which
its own `check_delivery_deployment.py` attests. The backed-up drop-ins of the predecessor sit in
`systemd-before/` of the evidence directory for the script's `rollback` mode.
