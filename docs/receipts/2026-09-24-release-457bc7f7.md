# Immutable release 457bc7f7 — production receipt

**Status: FINAL — 0.6.1 is live and production is at schema 057.**
Window `20260924T170546Z`. Evidence directory
`~/.local/state/brain-v42-delivery/457bc7f7980dfe67e77daa4f6d0370a68364a234/20260924T170546Z`.
Executed by `release-script-057.sh` (modes `build` → `prove` → `cutover`), retained in the
evidence directory and in `~/.local/state/brain-v42-delivery/handoff-2026-09-24b/`
(predecessor `44a13a7e`, `MEMBER_VERSION=0.4.0`, `PROBE_PR=199`).

This is the **schema-change path**, 056 → 057 (`search_log.embedding_model`).

## What it makes live

| PR | What it ships |
| --- | --- |
| **#197** | headless-agents 0.4.0 hardening (pre-tag review fixes, docs/changelog) |
| **#198** | `ha run` gives each link its own model, refused before running when none is picked |
| **#199** | migration 057 — `search_log.embedding_model` (TEXT, NULLABLE, NO DEFAULT, no backfill; ticket `4fac067a`, decision `1669d429`) + recovery contract v14 (base + `pg_restore` twin) |

Verified in the installed code, not in the diff:
`alembic/versions/057_search_log_embedding_model.py`, and `embedding_model` handling in
`db/tables.py`, `metrics/collector_db.py` and `mcp/tools/brain_tools.py`, are all present
under `releases/457bc7f7…/venv/lib/python3.12/site-packages/brain_v42/`; `ops/recovery/brain-v42-v14.json`
and its `pg_restore` twin ship in the release's source archive tree (`ops/recovery/` is not
part of the installed wheel).

**Not in this release**: lot A (PR #201, merged as `50a77858`) is not an ancestor of the SHA
this release was built from and has not landed.

## Release record

The release is `457bc7f7980dfe67e77daa4f6d0370a68364a234`, the merge commit of #199 into
`main`, built from a clean worktree detached at that SHA — not from the canonical checkout.
Predecessor `44a13a7e501bf8e63dc8e58868a8be48ec71d387`, measured on the eight `ExecStart` and
the `live_release_sha` fact at build time (2026-09-24 ~14:40 UTC).

| Input | Measured value |
| --- | --- |
| Version in `pyproject.toml` and `uv.lock` | `0.6.1`; `headless-agents` `0.4.0` (unchanged since 44a13a7e) |
| Build interpreter | `/opt/red-python/cpython-3.12.12/bin/python3.12` |
| Shipped Alembic head (smoke) | `057` |
| Production Alembic head | `056` before the window, `057` after |
| Pinned assets | migration 057, v13, v14 (base + `pg_restore` twin) and v11 ACL digests all matched the script's pins |
| Drop-ins | all eight byte-identical to the live ones with the SHA masked — `package_version` stayed `0.6.1`, so there is no other difference left to mask |

Artifact digests (SHA-256):

| Artifact | Digest |
| --- | --- |
| `brain_v42-0.6.1-py3-none-any.whl` | `0dc046d3cdf8f99d309b3423f2f77a3357cc64061e87afb266abcf09eac1ce96` |
| `headless_agents-0.4.0-py3-none-any.whl` | `4fa9bdf26831716e6be2f28408cdc0e149511cb56a401cfa66d5f7cfcd72e319` |
| `brain-v42.tar.gz` (source archive) | `db6b78b69639257742cf9cbbd56c62fd3a065f58c7d6449a62a8344967e41a81` |
| `delivery-release.json` (manifest) | `de06f84df361b0fdbe3924d0db9c98a7f55e6b1e182440b329131b942abc7510` |

The manifest binds 355 package payload files, 75 source payload files and the workspace
member's 28 (+1/+1/+1 over the 44a13a7e build: the one new migration file, plus whatever
#197/#198 touched in headless-agents, all carried by the same merge).

## Recovery proof (before touching production)

`prove`, 17:07:13 → 17:07:32 UTC, read-only for production:

- fresh owner-only `pg_dump -Fc` of production at 056, SHA-256
  `a83a435cfe627e1fed8ed509ef114608959b5771e7069f9206a695d06cb6734f`
  (globals `efc291ced1554f0b4ad4741a9a073e0895647237481cf775d63f2ec1c293ec77`);
- **real restore** into a disposable clone (pinned PG16 image, tmpfs, no port, no volume);
- clone **as restored** (056) proved by **v13: 30/30** — v13's `pg_restore` twin already
  carries `knowledge_claim_current` in its restored form since #185, so unlike v12 one
  generation earlier this needed no accounted divergence; ACL v11: zero failure;
- clone **migrated 056 → 057** with the installed wheel, proved by the **v14 twin: 30/30**;
  ACL v11: pass;
- the owned disposable container removed.

## Cutover

17:09:20 → 17:09:30 UTC (19:09 CEST — outside 06:00–09:00 and 13:00; `brain-v42-dream` was
`failed`/inactive, its timer due at 06:00 CEST the next day). Four active triggers frozen, the
eight writers stopped and each confirmed at `MainPID=0`, private targets attested, **Alembic
056 → 057 applied at 17:09:21Z** and post-checked (`search_log.embedding_model` text,
nullable, no default), the reviewed drop-ins published, `brain-mcp-http` and `brain-metrics`
started dormant, dormant preflight `status=ok` / `source_sha=457bc7f7…` /
`schema_revision=057`, triggers restored with the watchdog last, observer restarted.
`unit-state.before` and `unit-state.after` are identical. All eight writers measured on the
new SHA.

`brain-v42-dream.service` was `failed` before the window and is `failed` after: pre-existing,
neither caused nor repaired here.

## Registry change in the same window (ticket e565dc2c)

Ticket `e565dc2c` (runbook `434dc417`) added `hawkixs/red-life` to the delivery registry in the
same window, so the MCP dropped only once. The two candidate files — generated from GitHub
and Brain (block 0), validated with the live release's `DeliverySettings`, App canary passed
on `hawkixs/red-life` — were swapped in at 17:09:16Z, **just before** the cutover, whose own
starts picked them up: no separate restart. `delivery-observer.env` and `delivery-mcp.env`
both moved from 26 to 27 entries and from 50 to 52 repository mappings: the `red-life`
entry itself, plus `hawkixs/red-life` in the `red-lab-factory` aggregate; nothing removed.
Both files were backed up first (`*.bak-20260924-190916`, `0600`, still holding
`BRAIN_DELIVERY_POSTGRES_URL` and therefore kept private). The new registry is measured in
the live environment of `brain-mcp-http.service` (PID `2886371`) and
`brain-v42-delivery-observer.service` (PID `2886954`).

## Acceptance

- `/health`: `version 0.6.1`, `alembic_head 057`.
- `alembic_version`: `057`.
- `search_log.embedding_model`: `text`, nullable, no default — confirmed on production.
- `POST /mcp` without a bearer: `401`.
- A real `brain_search` call through MCP succeeded.
- `search_log` row `14127`, logged at `17:09:46Z`, carries `embedding_model=codestral-embed-2505`
  — the first production row this migration ever attributed.
- **DR contract v14 against production: 30/30**
  (`v14-production-result.json`, SHA-256
  `05abc24753dd3f3523a984bc9e6ea7d19ad29485b90731da3b17499591f5532f`); ACL v11 against
  production: 1/1.
- Zero `error` line in the new MCP, observer or metrics journals; the only `ERROR` observed is
  the old MCP process (PID `1070703`) logging its own stop at cutover — expected, not a
  finding.

## Rollback

`SOURCE_TREE=<worktree> PROBE_PR=199 release-script-057.sh rollback 457bc7f7980dfe67e77daa4f6d0370a68364a234 20260924T170546Z`
republishes the backed-up `44a13a7e` drop-ins and restarts the MCP and metrics. **Code-only:
057 stays applied**, and `44a13a7e`'s own preflight refuses 057 — there is no roll-forward
back to it. A real schema rollback means restoring the pre057 dump above.
