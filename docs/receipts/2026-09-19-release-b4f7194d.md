# Immutable release b4f7194d — production receipt

**Status: FINAL — the release is live; the first observation it was cut over for succeeded
five minutes after the window.**
Window `20260919T201620Z`. Evidence directory
`~/.local/state/brain-v42-delivery/b4f7194d84c153dad11ee249ec9eca8951056651/20260919T201620Z`.
Procedure: `docs/runbooks/2026-09-07-observable-delivery-workflows.md`, subsequent-release path
(no schema change), executed by the 2026-09-15 script with five anchors patched (predecessor,
head 054), retained as `release-script.sh` in the evidence directory and as
`~/.local/state/brain-v42-delivery/handoff-2026-09-19/`.

This release carries no schema change. It makes pull requests #154 (anyio 4.14.2 for
CVE-2026-63374 and CVE-2026-64847) and #155 reachable by the eight systemd writers. PR #155 is
the fix of Brain ticket `731ab364`: the delivery observer read a `url` key GitHub does not send on
pull-request review objects, so the first production contract requiring an approval (red-rail,
ticket `3f78854b`) failed every observation with `provider_invalid_response`; the fix moves the
fence to `pull_request_url`, ships a bounded diagnostic on `ProviderError` and one
`observation_error` JSON line on stderr per failed observation.

## Release record

The release is `b4f7194d84c153dad11ee249ec9eca8951056651`, the merge commit of pull request #155
into `main`. Its predecessor was `9bdb38122fb7603927f18d1aaec8544ae4b1300d` (migration 054,
2026-09-18), measured on the eight `90-immutable-release.conf` drop-ins and on the `ExecStart` of
`brain-mcp-http` before the build.

The merged `main` revision was verified on the host before the build: 10498 unit tests passed
(107 skipped), `ruff check`, `ruff format --check` and `mypy src/` clean, 230 observer unit and
integration tests passed against `brain_test` at head 054. Two independent reviews preceded the
pull request (red-reviewer: PATCH_THEN_SHIP, four findings closed; Codex gpt-5.6-terra:
PATCH_THEN_SHIP, one finding closed). Production was at 054 throughout.

| Input | Measured value |
| --- | --- |
| Version in `pyproject.toml` and `uv.lock` | `0.6.0`; `headless-agents` `0.2.0` |
| Build interpreter | `/opt/red-python/cpython-3.12.12/bin/python3.12` |
| Working tree | clean at `SOURCE_SHA` |
| Repository Alembic head | `054` — unchanged |
| Drop-ins | eight rendered `90-immutable-release.conf` and the observer unit byte-identical to the live ones with the SHA masked |
| `zz-` drop-ins at build and at cutover | none |

Artifact digests (SHA-256):

| Artifact | Digest |
| --- | --- |
| `brain_v42-0.6.0-py3-none-any.whl` | `a8edf11eee2067cfc63626c865f5faa64890bdf3c989a7652128d974f7714938` |
| `headless_agents-0.2.0-py3-none-any.whl` | `33dad2e1dcb6240e4105df6607401601ec1d56817c76dbcaf1749c364f28290a` |
| `brain-v42.tar.gz` (source archive) | `c863cf9fe79a9ea6f312366b276d93c8e26c4d71d98be880b2a4e5455f269782` |
| `delivery-release.json` (manifest) | `cacf6e277a9dc31a637f0a2f91abc4affabb94b1871f597d75fe3dc508ce1200` |

The smoke, run outside the checkout, asserted that `brain_v42` resolves inside the release venv,
that the installed versions are `0.6.0` and `0.2.0`, and that `shipped_alembic_head()` returns
`054`.

## Build and cutover

Build: 20:16:39 → 20:16:45 UTC, production untouched. A first `build` invocation at 20:16:20 UTC
stopped at the source checks because the checkout was on the feature branch, not on the merge
commit; nothing was written and the run was repeated from `main` at `b4f7194d`.

Cutover: 20:46:24 → 20:46:33 UTC (22:46 CEST) — quiesce of the active triggers and writers,
publication of the reviewed drop-ins, dormant start of `brain-mcp-http` and `brain-metrics`,
dormant preflight (`scripts/check_delivery_deployment.py`: `status=ok`, `schema_revision=054`),
triggers restored with the watchdog last, observer restarted because it was active before the
window. `unit-state.before` and `unit-state.after` are byte-identical (SHA-256
`a391e3193f9b1d49…` on both sides). All eight writers measured on the new SHA; `POST /mcp` without
a bearer answers `401`; `/health` of the dormant MCP reported `version 0.6.0`, `alembic_head 054`.

`brain-v42-dream.service` was in its `failed` state from the night of 2026-09-19 (Codex and
OpenCode Go both at their weekly usage limit, ticket `f4277a90`) throughout; its timer was
restored for 06:00 CEST on 2026-09-20.

## Acceptance: the observation the release was cut over for

Binding `44e08c97` (hawkixs/red-rail PR #3, contract revision 2 of ticket `3f78854b`) had failed
68 observations in a row and sat in a 300 s backoff. Its first attempt after the cutover ran at
20:51:37 UTC and finished at 20:51:39 UTC with `outcome=success`: the binding is `observed`, head
`6627f384`, `integration_sha` `6c16ada5`, and the `integration` receipt
`b26a9b5e-56aa-4892-ae43-985c6251b5ef` was issued at 20:51:39 UTC. The observer's journal since
the cutover holds no `observation_error` line. red-rail was notified.

## Rollback

`release-script.sh rollback b4f7194d84c153dad11ee249ec9eca8951056651 20260919T201620Z` republishes
the backed-up drop-ins of `9bdb3812` and restarts; nothing to restore in the database.
