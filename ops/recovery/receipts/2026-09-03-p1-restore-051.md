# P1 restore drill receipt — contract v8 against a real restore at head 051

The runbook's `dr-current` block is prose, and prose about an ABSENCE ("not
replayed", "not measured") is the one claim nothing can derive. This file is the
artifact that makes the positive claim derivable instead: a test reads it, so the
declaration and the evidence cannot drift apart in silence.

Fields below are parsed. Keep `key: value`, one per line.

    contract: brain-v42/postgresql-recovery/v8
    contract_version: v8
    kind: real-restore
    head: 051
    date: 2026-09-03
    base_twin: 30/30
    acl_twin: 1/1

## What was replayed, and against what

| | |
| --- | --- |
| Source dump | `/data/backups/20260903_010223/brain-v42.dump.gz` (rail `red-backup`, 03:02) |
| sha256 (.gz) | `c7baadce0562edd3941a197b7527f64710c23b62bced26351df73b418d5329ae` — equal to the run manifest's own checksum, verified rather than assumed |
| sha256 (decompressed, as restored) | `e4771acc58755dd469c12bf41e835daa381adf777b1791e04a6ce4433feb3b9f` |
| Head CONTAINED in the dump | `051`, read with `pg_restore --data-only --table=alembic_version` before restoring anything |
| Bench | throwaway container `brain_drill_20260903`, production image `pgvector/pgvector:pg16` (`sha256:b295c2aa9272`), data in tmpfs, destroyed afterwards |
| Restore command | `pg_restore --exit-on-error --clean --create --username=postgres --dbname=postgres` — the runbook's disaster command, WITHOUT `--no-owner --no-acl`, which is the whole point of the ACL twin |
| Result | `rc=0`, empty stderr, 4 s |

Measured ON the restored database, not expected: head `051`, 34 `public` base
tables, 3 341 `learnings`, 59 rows in `project_focus_history` (050's seed), 0 in
`brain_session_checkpoints`.

## Scores

    psql -U brain -d brain -Atq -v ON_ERROR_STOP=1 -f ops/recovery/brain-v42-v8-pgrestore.sql
      contract_id = brain-v42/postgresql-recovery/v8              SCORE = 30/30

    psql -U brain -d brain -Atq -v ON_ERROR_STOP=1 -f ops/recovery/brain-v42-v8-acl-pgrestore.sql
      contract_id = brain-v42/postgresql-recovery/v8-acl-pgrestore SCORE = 1/1
      observed: contract_grant_mismatches 0, relation_owner_mismatches 0,
                role_privilege_mismatches 0, unexpected_grantee_mismatches 0,
                tolerated_superuser_roles ["postgres"]

`sequence_shape` — the one check the runbook says a LIVE replay cannot exercise,
true by construction on a running database — passes here, at head 051.

`extension_versions` passes while REPORTING a difference: `origin_inventory`
`vector 0.8.2` against an observed `vector 0.8.5`. Since v6 the check is
`extension_inventory` with `restore_rule: names-only`, so a version drift is
visible in the receipt without gating it. The operator gesture it implies
(`ALTER EXTENSION vector UPDATE` on production) is still open, and its stated
precondition — proving the `0.8.2 -> 0.8.4` SQL delta empty — is now itself
stale: the image ships `0.8.5`.

## What this receipt does NOT attest

- No end-to-end operator rehearsal: a NEW throwaway cluster, not a production
  cluster put in quarantine. No backend termination, no canaries, no watchdog
  re-arming.
- Not the "archive behind the head" path: this dump was already at `051`, so
  `alembic upgrade head` after restoring was never exercised — and that is the
  likelier path in a real disaster.
- Nothing about duration: 4 s on 73 MB in tmpfs. The runbook's
  `timeout -s KILL 30m` was never approached, so it is still unvalidated.
- The bench cluster was EMPTY, so `--clean` met no live connection to terminate.

Full narrative: `w47-p1-restore-drill-051.md` in the 2026-09-03 orchestration
reports (outside this repository).
